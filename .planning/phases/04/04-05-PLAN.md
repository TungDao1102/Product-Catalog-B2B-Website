---
phase: 04
plan: 05
type: execute
wave: 3
depends_on:
  - 04-04
files_modified:
  - resources/views/products/index.blade.php
  - resources/views/products/show.blade.php
  - resources/views/categories/show.blade.php
  - resources/views/posts/index.blade.php
  - resources/views/posts/show.blade.php
  - resources/views/projects/index.blade.php
  - resources/views/projects/show.blade.php
  - resources/views/home/index.blade.php
  - resources/views/contact.blade.php
  - resources/views/partials/footer.blade.php
autonomous: false
requirements:
  - REQ-i18n-03

must_haves:
  truths:
    - "All frontend view hardcoded Vietnamese text replaced with `__()` calls"
    - "Breadcrumb navigations use `__()` for page titles"
    - "Empty state messages, button labels, form labels all translatable"
    - "Page header titles and descriptions translatable"
  artifacts:
    - path: "All view files under resources/views/"
      provides: "Translatable UI strings via `__()` calls referencing lang files"
  key_links:
    - from: "resources/views/*.blade.php"
      to: "lang/{locale}/*.php"
      via: "__('products.page_title')" etc.
---

<objective>
Replace all hardcoded Vietnamese text in all Blade views with `__()` helper calls referencing the lang files created in Plan 04-04.

Purpose: Every visible UI string becomes translatable — switch locale and all labels, titles, buttons, empty states, breadcrumbs change to the target language.
Output: 10 view files updated with translatable strings.
</objective>

<execution_context>
@C:/Users/daotu/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/daotu/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04/04-CONTEXT.md

# Read all current view files before modifying
@resources/views/products/index.blade.php
@resources/views/products/show.blade.php
@resources/views/categories/show.blade.php
@resources/views/posts/index.blade.php
@resources/views/posts/show.blade.php
@resources/views/projects/index.blade.php
@resources/views/projects/show.blade.php
@resources/views/home/index.blade.php
@resources/views/contact.blade.php
@resources/views/partials/footer.blade.php

@resources/views/partials/navbar.blade.php  # Already updated in Plan 04-04 — read to understand pattern
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: i18n for products/index.blade.php</name>
  <files>
    resources/views/products/index.blade.php
  </files>
  <action>
    Replace all hardcoded Vietnamese strings with `__()` calls:

    - `@section('title', 'Danh mục sản phẩm')` → `@section('title', __('products.page_title'))`
    - `@section('meta_description', 'Danh mục sản phẩm đa dạng, chất lượng cao cho doanh nghiệp')` → `@section('meta_description', __('products.page_description'))`
    - Page header h1 text
    - Breadcrumb: `Trang chủ` → `{{ __('navigation.home') }}`
    - Breadcrumb: `Sản phẩm` → `{{ __('navigation.products') }}`
    - Sidebar section: `Tìm kiếm` → `{{ __('navigation.search') }}`
    - Search placeholder: `Tìm sản phẩm...` → `{{ __('common.search_placeholder') }}`
    - Brand filter heading: `Hãng sản xuất`
    - Apply filters button: `Áp dụng bộ lọc` → `{{ __('common.apply_filters') }}`
    - "Hiển thị X-Y trên Z sản phẩm" section → reconstruct using `__('common.showing')`, `__('common.to')`, `__('common.of')`, `__('common.products_lower')`
    - Sort dropdown options: `Sắp xếp: Mặc định`, `Tên A-Z`, `Tên Z-A`, `Mới nhất`, `Cũ nhất`
    - Product card: `Chi tiết` button, `Báo giá` button
    - Empty state: `Không tìm thấy sản phẩm nào.` → `{{ __('common.no_products') }}`
    - Featured badge: `Nổi bật` → `{{ __('common.featured') }}`

    Each view section must use the correct lang key from the files created in Plan 04-04. If a string doesn't have a dedicated key yet, add it to the appropriate lang file first.

    Do NOT modify route names or URL parameters — only replace hardcoded display strings.
    Do NOT modify `@section('title', ...)` and `@section('meta_description', ...)` for dynamic content (e.g. `$product->name`) — only replace static strings.
  </action>
  <verify>
    <automated>
      php artisan tinker --execute="
        \$content = file_get_contents('resources/views/products/index.blade.php');
        \$remaining = preg_match_all('/[À-ỹ]{3,}/u', \$content, \$matches);
        echo \$remaining > 0 ? 'WARN: '.\$remaining.' Vietnamese chars remaining' : 'OK: No hardcoded Vietnamese';
      "
    </automated>
    <human-check>
      Visit `/vi/san-pham` — page renders with correct Vietnamese text. Visit `/en/san-pham` — page renders in English (search placeholder, sort labels, empty state). See also: `ProductCardRenderingTest.php` may need locale prefix in URL assertions (handled in Plan F).
    </human-check>
  </verify>
  <done>
    products/index.blade.php has all hardcoded strings replaced with `__()` calls.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: i18n for products/show.blade.php</name>
  <files>
    resources/views/products/show.blade.php
  </files>
  <action>
    Replace all hardcoded Vietnamese strings with `__()`:

    - Page header: `Chi tiết sản phẩm` → `{{ __('products.detail_title') }}`
    - Breadcrumb: `Trang chủ`, `Sản phẩm`
    - `Mô tả sản phẩm` heading → `{{ __('products.description') }}`
    - `Thông số kỹ thuật` heading
    - `Thông số` / `Giá trị` table headers
    - Brochure section: `Tải brochure sản phẩm`, description text, `Tải PDF`
    - `Mã sản phẩm:` prefix → `{{ __('products.product_code') }}:`
    - `Hãng sản xuất`, `Danh mục`, `Đơn vị tính`, `Số lượng tối thiểu`, `Tình trạng`, `Còn hàng`
    - `Yêu cầu báo giá` button
    - `Sản phẩm liên quan` heading
    - Quote modal: `Hủy` / `Gửi yêu cầu` buttons
    - Quote modal form labels
    - Gallery nav aria-labels (`Ảnh trước`, `Ảnh tiếp`)
    - Product card: `Chi tiết`, `Báo giá` buttons
    - `Tải brochure kỹ thuật` button
    - `Vui lòng kiểm tra lỗi thông tin:` alert heading
    - `Còn hàng` product status

    Add any missing lang keys to `lang/vi/products.php` and `lang/en/products.php` if needed.
  </action>
  <verify>
    <automated>
      php artisan tinker --execute="
        \$content = file_get_contents('resources/views/products/show.blade.php');
        \$remaining = preg_match_all('/[À-ỹ]{3,}/u', \$content, \$matches);
        echo \$remaining > 0 ? 'WARN: '.\$remaining.' Vietnamese chars remaining' : 'OK';
      "
    </automated>
    <human-check>Visit product detail page in both locales. Verify all UI strings translate.</human-check>
  </verify>
  <done>
    products/show.blade.php has all hardcoded strings replaced with `__()` calls.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: i18n for categories/show.blade.php, posts/*, projects/*, home/index, contact.blade.php, footer</name>
  <files>
    resources/views/categories/show.blade.php
    resources/views/posts/index.blade.php
    resources/views/posts/show.blade.php
    resources/views/projects/index.blade.php
    resources/views/projects/show.blade.php
    resources/views/home/index.blade.php
    resources/views/contact.blade.php
    resources/views/partials/footer.blade.php
  </files>
  <action>
    Apply the same i18n pattern as Tasks 1-2 to each of these view files. Read each file first, then replace all hardcoded Vietnamese display strings with `__()` calls.

    **Key patterns per view:**

    **categories/show.blade.php:**
    - Breadcrumb: `Trang chủ` → `__('navigation.home')`
    - Primary category filter heading
    - Search, sort dropdown, empty state
    - Product card buttons: `Chi tiết`, `Báo giá`
    - `Áp dụng bộ lọc` button

    **posts/index.blade.php:**
    - `@section('title', 'Tin tức')` → `{{ __('navigation.posts') }}`
    - `@section('meta_description', ...)` → `{{ __('seo.posts_title') }}`
    - Breadcrumb: `Trang chủ`, `Tin tức`
    - H1: `Tin tức`
    - `Đọc tiếp` button → `{{ __('common.read_more') }}`
    - Empty state: `Chưa có bài viết nào.`

    **posts/show.blade.php:**
    - Breadcrumb: `Trang chủ`, `Tin tức`
    - H1: `Tin tức` (page header)
    - `Quay lại danh sách tin tức` button
    - Footer pagination labels

    **projects/index.blade.php:**
    - `@section('title', 'Dự án')`
    - Breadcrumb: `Trang chủ`, `Dự án`
    - H1: `Dự án`
    - `Xem chi tiết` button
    - Empty state: `Chưa có dự án nào.`

    **projects/show.blade.php:**
    - Breadcrumb: `Trang chủ`, `Dự án`
    - H1: `Dự án` (page header)
    - `Quay lại danh sách dự án` button
    - Gallery nav aria-labels

    **home/index.blade.php:**
    - Carousel texts, section titles, descriptions
    - "Xem sản phẩm", "Khám phá ngay", "Đọc thêm" buttons
    - About section: "Về chúng tôi", "Giải pháp tối ưu cho doanh nghiệp", etc.
    - "Nổi bật" labels
    - Feature product section, latest product section
    - Article section
    - "Đăng ký nhận tin" section
    - Empty state texts

    **contact.blade.php:**
    - `@section('title', 'Liên hệ')` → `{{ __('navigation.contact') }}`
    - `@section('meta_description', ...)` → `{{ __('seo.contact_description') }}`
    - Breadcrumb: `Trang chủ`, `Liên hệ`
    - H1: `Liên hệ`
    - Section title: `Liên hệ`, `Gửi yêu cầu cho chúng tôi`
    - Form labels, submit button
    - Success flash message
    - Map area texts

    **footer.blade.php:**
    - Office info (address, phone, email)
    - Quick links (Trang chủ, Sản phẩm, Liên hệ)
    - Working hours section
    - Newsletter section
    - Copyright text
    - "Đăng ký" button
    - "Giờ làm việc" heading
    - "Liên kết nhanh" heading
    - "Văn phòng" heading

    Add any missing lang keys to the appropriate lang file. Keep the key naming consistent.
  </action>
  <verify>
    <automated>
      php artisan tinker --execute="
        \$files = [
          'categories/show.blade.php','posts/index.blade.php','posts/show.blade.php',
          'projects/index.blade.php','projects/show.blade.php',
          'home/index.blade.php','contact.blade.php','partials/footer.blade.php'
        ];
        \$total = 0;
        foreach (\$files as \$f) {
          \$content = file_get_contents('resources/views/'.\$f);
          \$remaining = preg_match_all('/[À-ỹ]{3,}/u', \$content, \$matches);
          \$total += \$remaining;
          echo \$f.': '.\$remaining.' remaining'.PHP_EOL;
        }
        echo 'TOTAL remaining: '.\$total;
      "
    </automated>
    <human-check>
      Visit `/vi` and `/en` — all pages render in correct language. Switch locales and verify all text changes.
    </human-check>
  </verify>
  <done>
    All 8 view files have hardcoded Vietnamese strings replaced with `__()` calls. No remaining hardcoded translatable strings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| lang/ files → view rendering | Language strings loaded from files to user-facing output |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | lang/ files | accept | lang files are application code, not user-modifiable |
</threat_model>

<verification>
1. Automated check: all view files have zero remaining Vietnamese-only character sequences outside of `__()` calls
2. Manual: browse every frontend route in both /vi and /en — all text renders in correct language
3. Check that `__()` calls with missing keys don't break the page (Laravel returns the key name as fallback)
</verification>

<success_criteria>
- All 10 view files updated with `__()` calls
- No hardcoded Vietnamese display strings remain in Blade views
- All lang keys used have corresponding entries in `lang/vi/` and `lang/en/`
- Pages render without errors in both locales
</success_criteria>

<output>
Create `.planning/phases/04/04-05-SUMMARY.md` when done
</output>
