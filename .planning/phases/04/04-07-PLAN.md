---
phase: 04
plan: 07
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - database/seeders/CategorySeeder.php
  - database/seeders/BrandSeeder.php
  - database/seeders/ProductSeeder.php
  - database/seeders/PostSeeder.php
  - database/seeders/ProjectSeeder.php
  - database/seeders/DatabaseSeeder.php
  - app/Http/Controllers/ProductController.php
  - app/Http/Controllers/CategoryController.php
  - app/Http/Controllers/HomeController.php
  - app/Http/Controllers/PostController.php
  - app/Http/Controllers/ProjectController.php
autonomous: true
requirements:
  - REQ-DB-02
  - REQ-i18n-01
  - REQ-i18n-04

must_haves:
  truths:
    - "All seeders use translatable array format ['vi' => '...', 'en' => '...'] for translatable fields"
    - "English translations exist alongside Vietnamese in seeders"
    - "ProductController and CategoryController search queries use name->vi and name->en JSON path syntax"
    - "HomeController uses getTranslation() for featured product names"
    - "Database can be re-seeded with bilingual content"
  artifacts:
    - path: "database/seeders/CategorySeeder.php"
      provides: "Bilingual category seed data with ['vi'=>..., 'en'=>...] format"
    - path: "database/seeders/BrandSeeder.php"
      provides: "Bilingual brand seed data with ['vi'=>..., 'en'=>...] format"
    - path: "database/seeders/ProductSeeder.php"
      provides: "Bilingual product seed data with ['vi'=>..., 'en'=>...] format"
    - path: "database/seeders/PostSeeder.php"
      provides: "Bilingual post seed data (title, content, excerpt) with ['vi'=>..., 'en'=>...] format"
    - path: "database/seeders/ProjectSeeder.php"
      provides: "Bilingual project seed data with ['vi'=>..., 'en'=>...] format"
    - path: "app/Http/Controllers/ProductController.php"
      provides: "Updated search queries using name->vi and name->en JSON path"
    - path: "app/Http/Controllers/CategoryController.php"
      provides: "Updated search queries using name->vi and name->en JSON path"
  key_links:
    - from: "seeders/*.php"
      to: "models/*.php"
      via: "Model::create(['name' => ['vi' => '...', 'en' => '...'], ...])"
    - from: "ProductController.php"
      to: "Product model"
      via: "where('name->vi', 'like', ...) orWhere('name->en', 'like', ...)"
---

<objective>
Update all 5 seeders to use translatable array format with English translations, fix search queries in ProductController and CategoryController for JSON column path syntax, and update HomeController for translatable attribute access.

Purpose: Seed data supports bilingual content, and existing search functionality works correctly with JSON translatable columns.
Output: Updated seeders with English translations, fixed controller queries.
</objective>

<execution_context>
@C:/Users/daotu/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/daotu/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04/04-RESEARCH.md
@.planning/phases/04/04-CONTEXT.md

# Seeders to update
@database/seeders/CategorySeeder.php
@database/seeders/BrandSeeder.php
@database/seeders/ProductSeeder.php
@database/seeders/PostSeeder.php
@database/seeders/ProjectSeeder.php
@database/seeders/DatabaseSeeder.php

# Controllers needing search query fixes
@app/Http/Controllers/ProductController.php
@app/Http/Controllers/CategoryController.php
@app/Http/Controllers/HomeController.php
@app/Http/Controllers/PostController.php
@app/Http/Controllers/ProjectController.php
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update CategorySeeder and BrandSeeder with bilingual data</name>
  <files>
    database/seeders/CategorySeeder.php
    database/seeders/BrandSeeder.php
  </files>
  <action>
    For **CategorySeeder.php**: Change every `'name' => '...'` to `'name' => ['vi' => '...', 'en' => 'English Translation']` and `'description' => '...'` to `'description' => ['vi' => '...', 'en' => 'English Translation']`.

    English translations for categories (translate each category name and description):
    - `Thiết bị an ninh soi chiếu` → `Security & Screening Equipment`
    - `Thiết bị báo cháy chữa cháy` → `Fire Alarm & Suppression Equipment`
    - `Thiết bị viễn thông truyền dẫn` → `Telecommunications & Transmission Equipment`
    - `Máy soi chiếu` → `X-Ray Scanners`
    - `Thiết bị kiểm soát an ninh` → `Access Control Equipment`
    - `Camera giám sát` → `Surveillance Cameras`
    - `Hệ thống báo cháy` → `Fire Alarm Systems`
    - `Bình chữa cháy` → `Fire Extinguishers`
    - `Thiết bị phòng cháy` → `Fire Protection Equipment`
    - `Thiết bị truyền dẫn` → `Transmission Equipment`
    - `Thiết bị mạng` → `Networking Equipment`
    - `Máy soi chiếu X-Ray` → `X-Ray Baggage Scanner`
    - `Máy dò kim loại` → `Metal Detectors`
    - `Cửa an ninh` → `Security Doors`
    - `Đầu đọc thẻ` → `Card Readers`
    - `Camera IP` → `IP Cameras`
    - `Camera Analog` → `Analog Cameras`
    - `Đầu báo cháy` → `Fire Detectors`
    - `Trung tâm báo cháy` → `Fire Alarm Panels`
    - `Bình chữa cháy bột` → `Powder Fire Extinguishers`
    - `Bình chữa cháy CO2` → `CO2 Fire Extinguishers`

    Translate descriptions similarly. Keep slug UNCHANGED — slug is non-translatable and shared per D-02.

    For **BrandSeeder.php**: Names stay as proper brand names (Hikvision, etc.) — they don't need translation. But `'description' => '...'` should become `'description' => ['vi' => '...', 'en' => 'English Translation']`.

    English brand descriptions: Translate the existing Vietnamese descriptions to English.
  </action>
  <verify>
    <automated>
      php artisan db:seed --class=CategorySeeder --force 2>&1
      php artisan db:seed --class=BrandSeeder --force 2>&1
      php artisan tinker --execute="
        \$c = App\Models\Category::first();
        echo 'VI: '.\$c->getTranslation('name', 'vi').' | EN: '.\$c->getTranslation('name', 'en');
      "
    </automated>
  </verify>
  <done>
    CategorySeeder and BrandSeeder create records with ['vi'=>..., 'en'=>...] format. getTranslations() returns both locales.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ProductSeeder with bilingual data</name>
  <files>
    database/seeders/ProductSeeder.php
  </files>
  <action>
    Update **ProductSeeder.php** to use translatable array format. For each product creation:

    - `'name' => '...'` → `'name' => ['vi' => 'Tên tiếng Việt', 'en' => 'English Name']`
    - `'short_description' => '...'` → `'short_description' => ['vi' => '...', 'en' => 'English description...']`
    - `'description' => '...'` → `'description' => ['vi' => '...', 'en' => 'English description...']`
    - `meta_title` and `meta_description` are nullable — set to `['vi' => '']` or generate from name for now.

    Keep `'slug'` unchanged — slug is shared (non-translatable) per D-02. Use Str::slug on the Vietnamese name for slug generation (same as before).

    Also update `'sku'` — keep unchanged.

    Add English translations for each product:
    - Name translations: `'Máy soi chiếu X-Ray trường bay'` → `'Airport X-Ray Scanner'`
    - `'Máy soi chiếu X-Ray di động'` → `'Portable X-Ray Scanner'`
    - `'Cửa dò kim loại 6 vùng'` → `'6-Zone Walk-Through Metal Detector'`
    - `'Camera IP Hikvision 2MP'` → `'Hikvision 2MP IP Camera'`
    - `'Camera IP Dahua 4MP AI'` → `'Dahua 4MP AI IP Camera'`
    - `'Camera Analog Hikvision 2MP'` → `'Hikvision 2MP Analog Camera'`
    - `'Đầu báo khói Honeywell'` → `'Honeywell Smoke Detector'`
    - `'Đầu báo nhiệt Honeywell'` → `'Honeywell Heat Detector'`
    - `'Tủ trung tâm báo cháy Honeywell 8 zone'` → `'Honeywell 8-Zone Fire Alarm Panel'`
    - `'Bình chữa cháy bột ABC 4kg'` → `'ABC 4kg Powder Fire Extinguisher'`
    - `'Bình chữa cháy CO2 5kg'` → `'CO2 5kg Fire Extinguisher'`
    - `'Cửa an ninh Bosch 3 cánh'` → `'Bosch 3-Wing Security Door'`
    - `'Đầu đọc thẻ RFID Bosch'` → `'Bosch RFID Card Reader'`

    Translate short_description and description for each product into English.

    The `'unit'` field stays as single string (non-translatable realm).
    The `'price'`, `'min_order_qty'`, `'images'`, `'brochure'`, and spec arrays stay unchanged — they are non-translatable fields.
  </action>
  <verify>
    <automated>
      php artisan db:seed --class=ProductSeeder --force 2>&1
      php artisan tinker --execute="
        \$p = App\Models\Product::where('sku', 'XRAY-BOSCH-001')->first();
        echo 'VI: '.\$p->getTranslation('name', 'vi').' | EN: '.\$p->getTranslation('name', 'en');
      "
    </automated>
  </verify>
  <done>
    ProductSeeder creates products with bilingual names, short_descriptions, and descriptions. Products have both locale translations.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update PostSeeder and ProjectSeeder with bilingual data + DatabaseSeeder reorder</name>
  <files>
    database/seeders/PostSeeder.php
    database/seeders/ProjectSeeder.php
    database/seeders/DatabaseSeeder.php
  </files>
  <action>
    **PostSeeder.php:** Change post creation to use translatable arrays:
    - `'title' => '...'` → `'title' => ['vi' => '...', 'en' => 'English Title']`
    - `'content' => '...'` → `'content' => ['vi' => '...', 'en' => 'English content...']`
    - `'excerpt' => '...'` → `'excerpt' => ['vi' => '...', 'en' => 'English excerpt...']`

    Translate existing Vietnamese content to English. Keep slug unchanged.

    **ProjectSeeder.php:** Change project creation to use translatable arrays:
    - `'title' => '...'` → `'title' => ['vi' => '...', 'en' => 'English Title']`
    - `'description' => '...'` → `'description' => ['vi' => '...', 'en' => 'English Description']`
    - `'content' => '...'` → `'content' => ['vi' => '...', 'en' => 'English Content...']`

    Translate existing content to English. Keep slug unchanged.

    **DatabaseSeeder.php:** No structural changes needed — it calls all seeders in order and that remains valid. However, ensure the seed order runs CategorySeeder → BrandSeeder → ProductSeeder → PostSeeder → ProjectSeeder (already the correct order).

    Read the existing PostSeeder and ProjectSeeder files first to understand their content, then add English translations for each record.
  </action>
  <verify>
    <automated>
      php artisan db:seed --class=PostSeeder --force 2>&1
      php artisan db:seed --class=ProjectSeeder --force 2>&1
      php artisan tinker --execute="
        \$post = App\Models\Post::first();
        echo 'VI: '.\$post->getTranslation('title', 'vi').' | EN: '.\$post->getTranslation('title', 'en');
      "
    </automated>
  </verify>
  <done>
    PostSeeder and ProjectSeeder create bilingual records. Database can be fully re-seeded.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix search queries in ProductController and CategoryController + add getTranslation in HomeController</name>
  <files>
    app/Http/Controllers/ProductController.php
    app/Http/Controllers/CategoryController.php
    app/Http/Controllers/HomeController.php
    app/Http/Controllers/PostController.php
    app/Http/Controllers/ProjectController.php
  </files>
  <action>
    **ProductController.php - index() method:**
    Find the search block (lines 25-31 currently):
    ```php
    if ($search = request('search')) {
        $query->where(function ($q) use ($search) {
            $q->where('name', 'like', "%{$search}%")
              ->orWhere('sku', 'like', "%{$search}%")
              ->orWhere('short_description', 'like', "%{$search}%");
        });
    }
    ```
    Change to use MySQL JSON path syntax:
    ```php
    if ($search = request('search')) {
        $query->where(function ($q) use ($search) {
            $q->where('name->vi', 'like', "%{$search}%")
              ->orWhere('name->en', 'like', "%{$search}%")
              ->orWhere('sku', 'like', "%{$search}%")
              ->orWhere('short_description->vi', 'like', "%{$search}%")
              ->orWhere('short_description->en', 'like', "%{$search}%");
        });
    }
    ```

    Also change `orderBy('name')` and `orderBy('name', 'desc')` to use JSON path:
    ```php
    case 'name_asc':
        $query->orderBy('name->vi');
        break;
    case 'name_desc':
        $query->orderBy('name->vi', 'desc');
        break;
    ```

    **CategoryController.php - show() method:**
    Apply the same search query changes (lines 38-44 currently):
    ```php
    if ($search = request('search')) {
        $query->where(function ($q) use ($search) {
            $q->where('name', 'like', "%{$search}%")
              ->orWhere('sku', 'like', "%{$search}%")
              ->orWhere('short_description', 'like', "%{$search}%");
        });
    }
    ```
    Change to:
    ```php
    if ($search = request('search')) {
        $query->where(function ($q) use ($search) {
            $q->where('name->vi', 'like', "%{$search}%")
              ->orWhere('name->en', 'like', "%{$search}%")
              ->orWhere('sku', 'like', "%{$search}%")
              ->orWhere('short_description->vi', 'like', "%{$search}%")
              ->orWhere('short_description->en', 'like', "%{$search}%");
        });
    }
    ```

    Also change `orderBy('name')` to `orderBy('name->vi')` in the sort options.

    **HomeController.php - index() method:**
    The featured products query uses `->where('is_active', true)` and `->where('is_featured', true)` — these are non-translatable boolean fields so no JSON path change needed. No search query exists here. Good.

    **PostController.php:**
    Posts don't have name-based search — they use slug for show() and published_at for index(). No changes needed for SEO data — that's handled in Plan 04-06.

    **ProjectController.php:**
    Projects don't have name-based search. No changes needed.

    IMPORTANT: Per Research finding #3: `->where('name', 'like', ...)` WILL BREAK after JSON migration because MySQL cannot do LIKE on a JSON column directly. The `->where('name->vi', 'like', ...)` syntax uses MySQL's JSON_UNQUOTE(JSON_EXTRACT(...)) internally, which works correctly.

    Also add `use Illuminate\Support\Facades\URL;` import if not present in controllers that need URL::defaults() approach. For controllers that use `route()` helper, the SetLocale middleware already sets URL::defaults, so `route('products.show', ['slug' => $product->slug])` works without locale parameter.
  </action>
  <verify>
    <automated>
      php artisan tinker --execute="
        \$content = file_get_contents('app/Http/Controllers/ProductController.php');
        echo str_contains(\$content, \"->where('name->vi'\") ? 'OK: JSON path query in ProductController' : 'MISSING: old where() style present';
      "
    </automated>
    <human-check>
      Run `php artisan test --filter=ProductSearchTest` — search functionality still works.
    </human-check>
  </verify>
  <done>
    ProductController and CategoryController use `name->vi` and `name->en` JSON path syntax for search and sort queries. All controllers handle translatable fields correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller→DB query | User search input used in LIKE queries with JSON path extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure | LIKE queries | mitigate | Search input is parameterized via Laravel query builder — protected from SQL injection |
| T-04-07-02 | Tampering | JSON path injection | accept | The `->` path syntax is parsed by MySQL's JSON_EXTRACT, not by PHP — no injection vector |
</threat_model>

<verification>
1. `php artisan db:seed --force` — all seeders run without errors
2. `php artisan tinker --execute="echo App\Models\Category::first()->getTranslations('name');"` shows both vi and en keys
3. Visit `/vi/san-pham?search=camera` — returns results (searches in Vietnamese name)
4. Visit `/en/san-pham?search=camera` — returns results (searches in English name)
5. Product sort by name works in both locales
6. `php artisan test --filter=ProductSearchTest` passes
</verification>

<success_criteria>
- All seeders use translatable array format `['vi' => '...', 'en' => '...']`
- English translations exist for all seeded content
- Search queries in ProductController and CategoryController use JSON path `name->vi` and `name->en`
- Sort by name uses `name->vi`
- Database fully re-seedable with bilingual content
- Existing search tests pass after migration
</success_criteria>

<output>
Create `.planning/phases/04/04-07-SUMMARY.md` when done
</output>
