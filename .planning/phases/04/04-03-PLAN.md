---
phase: 04
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/Filament/Resources/Categories/CategoryResource.php
  - app/Filament/Resources/Categories/Pages/CreateCategory.php
  - app/Filament/Resources/Categories/Pages/EditCategory.php
  - app/Filament/Resources/Brands/BrandResource.php
  - app/Filament/Resources/Brands/Pages/CreateBrand.php
  - app/Filament/Resources/Brands/Pages/EditBrand.php
  - app/Filament/Resources/Products/ProductResource.php
  - app/Filament/Resources/Products/Pages/CreateProduct.php
  - app/Filament/Resources/Products/Pages/EditProduct.php
  - app/Filament/Resources/Posts/PostResource.php
  - app/Filament/Resources/Posts/Pages/CreatePost.php
  - app/Filament/Resources/Posts/Pages/EditPost.php
  - app/Filament/Resources/Projects/ProjectResource.php
  - app/Filament/Resources/Projects/Pages/CreateProject.php
  - app/Filament/Resources/Projects/Pages/EditProject.php
autonomous: false
requirements:
  - REQ-i18n-02

must_haves:
  truths:
    - "All 5 Filament Resources have tabbed locale interface for translatable fields"
    - "Create and Edit pages show LocaleSwitcher action with VI|EN tabs"
    - "Slug field remains single, non-translatable, with auto-generation on name input"
    - "Non-translatable fields (SKU, price, images, technical_specs, is_published, etc.) appear unchanged"
    - "Translatable fields render correctly per locale tab switching"
  artifacts:
    - path: "app/Filament/Resources/Categories/CategoryResource.php"
      provides: "Translatable concern + getTranslatableLocales()"
    - path: "app/Filament/Resources/Categories/Pages/CreateCategory.php"
      provides: "CreateRecord\Concerns\Translatable + LocaleSwitcher action"
    - path: "app/Filament/Resources/Categories/Pages/EditCategory.php"
      provides: "EditRecord\Concerns\Translatable + LocaleSwitcher action"
    - path: "[Same pattern for all 5 Resources]"
      provides: "Translatable UI for each model"
  key_links:
    - from: "CategoryResource.php (Translatable concern)"
      to: "CreateCategory.php + EditCategory.php (LocaleSwitcher)"
    - from: "BrandResource.php (Translatable concern)"
      to: "CreateBrand.php + EditBrand.php (LocaleSwitcher)"
    - from: "ProductResource.php (Translatable concern)"
      to: "CreateProduct.php + EditProduct.php (LocaleSwitcher)"
    - from: "PostResource.php (Translatable concern)"
      to: "CreatePost.php + EditPost.php (LocaleSwitcher)"
    - from: "ProjectResource.php (Translatable concern)"
      to: "CreateProject.php + EditProject.php (LocaleSwitcher)"
---

<objective>
Add Filament's spatie-laravel-translatable-plugin to all 5 Resources: Category, Brand, Product, Post, Project. Enable tabbed locale switching for translatable fields. Keep slug auto-generation working on the active locale tab.

Purpose: Admin users can input content in both Vietnamese and English through a tabbed interface within each Resource's Create/Edit form.
Output: 15 files modified (5 Resources + 5 Create pages + 5 Edit pages) with Translatable concern and LocaleSwitcher.
</objective>

<execution_context>
@C:/Users/daotu/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/daotu/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04/04-RESEARCH.md
@.planning/phases/04/04-CONTEXT.md

# All existing Resource files (read current structure before modifying)
@app/Filament/Resources/Categories/CategoryResource.php
@app/Filament/Resources/Categories/Pages/CreateCategory.php
@app/Filament/Resources/Categories/Pages/EditCategory.php
@app/Filament/Resources/Brands/BrandResource.php
@app/Filament/Resources/Brands/Pages/CreateBrand.php
@app/Filament/Resources/Brands/Pages/EditBrand.php
@app/Filament/Resources/Products/ProductResource.php
@app/Filament/Resources/Products/Pages/CreateProduct.php
@app/Filament/Resources/Products/Pages/EditProduct.php
@app/Filament/Resources/Posts/PostResource.php
@app/Filament/Resources/Posts/Pages/CreatePost.php
@app/Filament/Resources/Posts/Pages/EditPost.php
@app/Filament/Resources/Projects/ProjectResource.php
@app/Filament/Resources/Projects/Pages/CreateProject.php
@app/Filament/Resources/Projects/Pages/EditProject.php
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Translatable concern to all 5 Resources</name>
  <files>
    app/Filament/Resources/Categories/CategoryResource.php
    app/Filament/Resources/Brands/BrandResource.php
    app/Filament/Resources/Products/ProductResource.php
    app/Filament/Resources/Posts/PostResource.php
    app/Filament/Resources/Projects/ProjectResource.php
  </files>
  <action>
    For each Resource file (CategoryResource, BrandResource, ProductResource, PostResource, ProjectResource):

    1. Add import: `use Filament\Resources\Concerns\Translatable;`
    2. Add trait: `use Translatable;` inside the class body
    3. Add method:
    ```php
    public static function getTranslatableLocales(): array
    {
        return ['vi', 'en'];
    }
    ```

    Do NOT modify any existing form/table configurations — the Filament plugin auto-detects translatable fields from the model's $translatable array and adds locale tabs automatically. Translatable fields defined in Form schemas remain as-is; the plugin wraps them in locale tabs.

    Per Research finding #6: Filament plugin does NOT require form schema changes — just add Translatable concern + LocaleSwitcher action.
  </action>
  <verify>
    <automated>
      php artisan tinker --execute="
        foreach (['CategoryResource','BrandResource','ProductResource','PostResource','ProjectResource'] as \$r) {
          \$c = 'App\\Filament\\Resources\\'.(\$r === 'CategoryResource' ? 'Categories\\' : (\$r === 'BrandResource' ? 'Brands\\' : (\$r === 'ProductResource' ? 'Products\\' : (\$r === 'PostResource' ? 'Posts\\' : 'Projects\\')))).\$r;
          \$rc = new ReflectionClass(\$c);
          \$t = \$rc->getTraitNames();
          echo \$r.': '.(in_array('Filament\\Resources\\Concerns\\Translatable', \$t) ? 'OK' : 'MISSING').PHP_EOL;
        }
      "
    </automated>
  </verify>
  <done>
    All 5 Resource classes use Translatable concern. Each has getTranslatableLocales() returning ['vi', 'en'].
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Translatable concern to Create pages + LocaleSwitcher action</name>
  <files>
    app/Filament/Resources/Categories/Pages/CreateCategory.php
    app/Filament/Resources/Brands/Pages/CreateBrand.php
    app/Filament/Resources/Products/Pages/CreateProduct.php
    app/Filament/Resources/Posts/Pages/CreatePost.php
    app/Filament/Resources/Projects/Pages/CreateProject.php
  </files>
  <action>
    For each Create page:
    1. Add import: `use Filament\Resources\Pages\CreateRecord\Concerns\Translatable;`
    2. Add trait: `use Translatable;` inside the class body
    3. Add `protected function getHeaderActions(): array` if not present:
    ```php
    protected function getHeaderActions(): array
    {
        return [
            \Filament\Actions\LocaleSwitcher::make(),
        ];
    }
    ```
    
    For CreateProduct.php which already has `afterSave()`, add the trait alongside existing code — the trait and afterSave() coexist. The trait MUST be added at the class level, not in a method.

    The LocaleSwitcher action renders as VI|EN tab buttons in the header that switch the active locale for translatable form fields.

    Per D-10: "Filament tabbed interface via LocaleSwitcher plugin" — this task implements that decision.
  </action>
  <verify>
    <automated>
      php artisan tinker --execute="
        foreach (['CreateCategory','CreateBrand','CreateProduct','CreatePost','CreateProject'] as \$p) {
          \$ns = 'App\\Filament\\Resources\\'.
            (in_array(\$p, ['CreateCategory']) ? 'Categories\\Pages\\' :
            (in_array(\$p, ['CreateBrand']) ? 'Brands\\Pages\\' :
            (in_array(\$p, ['CreateProduct']) ? 'Products\\Pages\\' :
            (in_array(\$p, ['CreatePost']) ? 'Posts\\Pages\\' : 'Projects\\Pages\\')))).\$p;
          echo \$p.': '.(class_exists(\$ns) ? 'OK' : 'MISSING').PHP_EOL;
        }
      "
    </automated>
  </verify>
  <done>
    All 5 Create pages have Translatable concern and LocaleSwitcher action in header.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add Translatable concern to Edit pages + LocaleSwitcher action</name>
  <files>
    app/Filament/Resources/Categories/Pages/EditCategory.php
    app/Filament/Resources/Brands/Pages/EditBrand.php
    app/Filament/Resources/Products/Pages/EditProduct.php
    app/Filament/Resources/Posts/Pages/EditPost.php
    app/Filament/Resources/Projects/Pages/EditProject.php
  </files>
  <action>
    For each Edit page:
    1. Add import: `use Filament\Resources\Pages\EditRecord\Concerns\Translatable;`
    2. Add trait: `use Translatable;` inside the class body
    3. Add LocaleSwitcher to the header actions array. For pages that already have `getHeaderActions()` (like EditProduct with DeleteAction), append `\Filament\Actions\LocaleSwitcher::make()` to the existing array:
    ```php
    protected function getHeaderActions(): array
    {
        return [
            \Filament\Actions\LocaleSwitcher::make(),
            \Filament\Actions\DeleteAction::make(),
        ];
    }
    ```
    For pages without getHeaderActions (like EditCategory, EditBrand), add the full method as shown above.

    The EditProduct keeps its existing `afterSave()` method — it's compatible with the Translatable trait.

    Per D-10: The LocaleSwitcher works identically on Edit pages, loading stored translations for each locale.
  </action>
  <verify>
    <automated>
      php artisan tinker --execute="
        foreach (['EditCategory','EditBrand','EditProduct','EditPost','EditProject'] as \$p) {
          \$ns = 'App\\Filament\\Resources\\'.
            (in_array(\$p, ['EditCategory']) ? 'Categories\\Pages\\' :
            (in_array(\$p, ['EditBrand']) ? 'Brands\\Pages\\' :
            (in_array(\$p, ['EditProduct']) ? 'Products\\Pages\\' :
            (in_array(\$p, ['EditPost']) ? 'Posts\\Pages\\' : 'Projects\\Pages\\')))).\$p;
          echo \$p.': '.(class_exists(\$ns) ? 'OK' : 'MISSING').PHP_EOL;
        }
      "
    </automated>
  </verify>
  <done>
    All 5 Edit pages have Translatable concern and LocaleSwitcher action.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin→filament form | Admin inputs translatable content per locale tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | LocaleSwitcher | accept | LocaleSwitcher only switches active tab; locale value is validated server-side through spatie's HasTranslations |
| T-04-03-02 | Information Disclosure | Translatable concern | accept | All translatable content is equally accessible per user's session auth; no privilege boundary crossed |
</threat_model>

<verification>
1. Visit `/admin/categories/create` — VI|EN tabs visible in the header
2. Click EN tab — form fields change to English input mode
3. Visit `/admin/products/1/edit` — LocaleSwitcher visible, Slug field is NOT translated (single input)
4. Non-translatable fields (SKU, price, images, etc.) render only once
5. Form submit saves correctly for active locale
</verification>

<success_criteria>
- Translatable concern added to all 5 Resources
- LocaleSwitcher action on all 5 Create pages and 5 Edit pages
- Slug auto-generation from name works on the active locale tab (Create only)
- Non-translatable fields remain single instances
- Admin routes work at /admin/* without locale prefix
</success_criteria>

<output>
Create `.planning/phases/04/04-03-SUMMARY.md` when done
</output>
