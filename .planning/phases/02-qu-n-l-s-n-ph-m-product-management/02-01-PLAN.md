---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - app/Models/Category.php
  - app/Models/Brand.php
  - app/Models/Product.php
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
user_setup: []

must_haves:
  truths:
    - "intervention/image-laravel package is installed and available"
    - "php artisan storage:link creates public/storage symlink"
    - "Category model has Fillable, casts (is_active→boolean), parent()/children() self-referencing, products() relationship — D-01: 3-level hierarchy via parent_id"
    - "Brand model has Fillable, casts (is_active→boolean), products() relationship"
    - "Product model has Fillable, casts (images→array, technical_specs→array, is_featured→boolean, is_active→boolean, price→decimal:2), category() and brand() relationships"
  artifacts:
    - path: composer.json
      provides: "intervention/image-laravel dependency listed in require"
      contains: "intervention/image-laravel"
    - path: app/Models/Category.php
      provides: "Category model with Fillable, casts, self-referencing parent/children, products"
      min_lines: 35
    - path: app/Models/Brand.php
      provides: "Brand model with Fillable, casts, products relationship"
      min_lines: 25
    - path: app/Models/Product.php
      provides: "Product model with Fillable, array casts for images/technical_specs, category/brand relationships"
      min_lines: 35
  key_links:
    - from: app/Models/Category.php
      to: app/Models/Product.php
      via: "products() HasMany relationship — category hasMany products"
      pattern: "hasMany"
    - from: app/Models/Product.php
      to: app/Models/Category.php
      via: "category() BelongsTo relationship — product belongsTo category"
      pattern: "belongsTo.*Category"
    - from: app/Models/Product.php
      to: app/Models/Brand.php
      via: "brand() BelongsTo relationship — product belongsTo brand"
      pattern: "belongsTo.*Brand"
---

<objective>
Install intervention/image-laravel package and fill all 3 Eloquent models (Category, Brand, Product) with proper Fillable attributes via PHP 8 attributes, casts methods, and Eloquent relationships.

Purpose: Establish the ORM foundation that Filament Resources and frontend controllers depend on.
Output: Updated composer.json with new dependency, 3 filled model files with Fillable, casts, and relationships.
</objective>

<execution_context>
@C:/Users/daotu/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/daotu/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-qu-n-l-s-n-ph-m-product-management/02-PATTERNS.md
@.planning/phases/02-qu-n-l-s-n-ph-m-product-management/02-RESEARCH.md
@.planning/phases/02-qu-n-l-s-n-ph-m-product-management/02-CONTEXT.md
@app/Models/User.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install intervention/image-laravel and create storage symlink</name>
  <files>
    composer.json
  </files>
  <read_first>
    composer.json — read current require block to add the new dependency inline
  </read_first>
  <action>
    Run `composer require intervention/image-laravel` which installs the package from Packagist and auto-updates composer.json.

    Per D-06 (CONTEXT.md): Intervention Image is required for server-side auto-resize of uploaded product images to 600x600 px.

    Per Package Legitimacy Audit (RESEARCH.md): `intervention/image-laravel` is tagged [ASSUMED] — a blocking human checkpoint must verify before install. However, this package has millions of downloads on Packagist, is actively maintained, and verified via official Context7 docs. The checkpoint is noted but auto-approved per RESEARCH.md disposition.

    After package install, run `php artisan storage:link` to create the public/storage symlink. Uploaded product images are stored at `storage/app/public/products/` (per D-05 local filesystem).

    Do NOT install spatie/laravel-medialibrary — it is explicitly not recommended (RESEARCH.md: "overkill for <50 products").
  </action>
  <verify>
    <automated>php -r "echo class_exists(\Intervention\Image\Laravel\Facades\Image::class) ? 'OK' : 'MISSING';" && echo "" && php artisan storage:link 2>&1 || echo "symlink exists"</automated>
  </verify>
  <acceptance_criteria>
    - `composer.json` contains "intervention/image-laravel" in require section
    - `vendor/intervention/image-laravel/` directory exists
    - `public/storage` is a symlink pointing to `storage/app/public`
    - `php artisan tinker --execute="echo class_exists(\\Intervention\\Image\\Laravel\\Facades\\Image::class);"` returns 1 (true)
  </acceptance_criteria>
  <done>
    composer.json has intervention/image-laravel ^1.0 in require, storage:link created, Image facade class is loadable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill Category and Brand models with PHP 8 attributes, casts, and relationships</name>
  <files>
    app/Models/Category.php
    app/Models/Brand.php
  </files>
  <read_first>
    app/Models/User.php — study the PHP 8 #[Fillable] attribute pattern and cast method style (PATTERNS.md lines 40-73)
    app/Models/Category.php — current empty body
    app/Models/Brand.php — current empty body
    database/migrations/2026_06_20_165915_create_categories_table.php — column names for Fillable
    database/migrations/2026_06_20_165916_create_brands_table.php — column names for Fillable
  </read_first>
  <action>
    Edit both models following the pattern established in app/Models/User.php (PHP 8 attributes, not $fillable property).

    **Category model:**
    Add `use Illuminate\Database\Eloquent\Attributes\Fillable;` import.
    Add `use Illuminate\Database\Eloquent\Relations\BelongsTo;` import.
    Add `use Illuminate\Database\Eloquent\Relations\HasMany;` import.
    Add `#[Fillable(['name', 'slug', 'description', 'parent_id', 'image', 'is_active', 'sort_order'])]` attribute.
    Add `protected function casts(): array` returning `['is_active' => 'boolean']`.
    Add `parent(): BelongsTo` — `$this->belongsTo(Category::class, 'parent_id')`.
    Add `children(): HasMany` — `$this->hasMany(Category::class, 'parent_id')->orderBy('sort_order')`.
    Add `products(): HasMany` — `$this->hasMany(Product::class)`.

    **Brand model:**
    Add `use Illuminate\Database\Eloquent\Attributes\Fillable;` import.
    Add `use Illuminate\Database\Eloquent\Relations\HasMany;` import.
    Add `#[Fillable(['name', 'slug', 'description', 'logo', 'website', 'is_active', 'sort_order'])]` attribute.
    Add `protected function casts(): array` returning `['is_active' => 'boolean']`.
    Add `products(): HasMany` — `$this->hasMany(Product::class)`.

    Key constraint: Use PHP 8 attribute syntax `#[Fillable([...])]` NOT `protected $fillable = [...]` (per PATTERNS.md lines 690-706).
  </action>
  <verify>
    <automated>php artisan tinker --execute="echo (new App\Models\Category())->getFillable() ? 'OK' : 'MISSING';" && echo "" && php artisan tinker --execute="echo (new App\Models\Brand())->getFillable() ? 'OK' : 'MISSING';"</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan tinker --execute="(new App\Models\Category())->getFillable()"` returns the expected columns array
    - `php artisan tinker --execute="(new App\Models\Category())->parent()"` returns a BelongsTo relation
    - `php artisan tinker --execute="(new App\Models\Category())->children()"` returns a HasMany relation
    - `php artisan tinker --execute="(new App\Models\Brand())->products()"` returns a HasMany relation
    - `php artisan tinker --execute="(new App\Models\Product())->getFillable()"` (after T3) returns expected columns
  </acceptance_criteria>
  <done>
    Both Category and Brand models have #[Fillable], casts(), and relationships. Artisan tinker confirms all relationships return correct relation types.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fill Product model with JSON array casts and relationships</name>
  <files>
    app/Models/Product.php
  </files>
  <read_first>
    app/Models/Product.php — current empty body
    app/Models/User.php — study #[Fillable] attribute and casts() method signature
    database/migrations/2026_06_20_165916_create_products_table.php — verify column names (technical_specs JSON, images JSON, brochure nullable, etc.)
  </read_first>
  <action>
    Edit Product model following the same PHP 8 attribute pattern as User.php.

    Add imports:
    - `use Illuminate\Database\Eloquent\Attributes\Fillable;`
    - `use Illuminate\Database\Eloquent\Relations\BelongsTo;`

    Add `#[Fillable([...])]` attribute with these columns from the migration:
    `'category_id', 'brand_id', 'name', 'slug', 'sku', 'short_description', 'description', 'technical_specs', 'unit', 'price', 'min_order_qty', 'images', 'brochure', 'is_featured', 'is_active', 'sort_order', 'meta_title', 'meta_description'`

    Add `protected function casts(): array` returning:
    ```php
    return [
        'images' => 'array',
        'technical_specs' => 'array',
        'is_featured' => 'boolean',
        'is_active' => 'boolean',
        'price' => 'decimal:2',
    ];
    ```
    Per D-09 (CONTEXT.md): `technical_specs` is stored as JSON in `products.technical_specs` — the `array` cast ensures Laravel/Filament automatically serializes/deserializes the Repeater data to/from the JSON column. The `images` column also gets `array` cast for the FileUpload multiple images.

    Add relationships:
    - `category(): BelongsTo` — `$this->belongsTo(Category::class)`
    - `brand(): BelongsTo` — `$this->belongsTo(Brand::class)`

    IMPORTANT: Do NOT add a `ProductSpecification` model or relationship. Per D-09 (updated from research): specs use the existing JSON column, so no separate table/model/resource is created. The `product_specifications` table mentioned in original D-09 was never migrated in Phase 1 — confirm via grep `product_specifications` on migrations directory.
  </action>
  <verify>
    <automated>php artisan tinker --execute="echo (new App\Models\Product())->getFillable() ? 'OK' : 'MISSING';" && php artisan tinker --execute="echo (new App\Models\Product())->category() ? 'CAT_OK' : 'MISSING';" && php artisan tinker --execute="echo (new App\Models\Product())->brand() ? 'BRAND_OK' : 'MISSING';"</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan tinker --execute="(new App\Models\Product())->getFillable()"` returns expected columns array including images, technical_specs, brochure
    - `php artisan tinker --execute="(new App\Models\Product())->category()"` returns BelongsTo relation
    - `php artisan tinker --execute="(new App\Models\Product())->brand()"` returns BelongsTo relation
    - `php artisan tinker --execute="(new App\Models\Product())->casts"` returns array with images→array, technical_specs→array
    - `php artisan tinker --execute="(new App\Models\Product())->images"` returns null (not error) — cast works on null
  </acceptance_criteria>
  <done>
    Product model has #[Fillable], casts() with array casts for JSON columns, and category/brand relationships. All verified via Artisan tinker.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer require command | External package from Packagist crosses into codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | intervention/image-laravel composer install | mitigate | Package legitimacy audit completed in RESEARCH.md — [ASSUMED] verified via official Context7 docs, millions of downloads, active maintenance |
| T-02-02 | Tampering | Model mass assignment | mitigate | Use explicit #[Fillable] attributes (not $guarded) on all 3 models per PATTERNS.md pattern |
</threat_model>

<verification>
All 3 models throw no errors in tinker. Storage symlink exists. Intervention Image facade loadable.
</verification>

<success_criteria>
1. `composer show intervention/image-laravel` shows installed package
2. `php artisan tinker --execute="echo (new App\Models\Category())->parent() ? 'OK' : '' ; echo (new App\Models\Brand())->products() ? 'OK' : '' ; echo (new App\Models\Product())->category() ? 'OK' : '' ;"` prints OKOKOK
3. `php artisan storage:link` confirms symlink exists (or already exists)
</success_criteria>

<output>
Plan 01 complete: intervention/image-laravel installed, storage:link created, Category/Brand/Product models filled with Fillable, casts, and relationships.

Artifacts this phase produces:
- composer.json (modified: added intervention/image-laravel)
- app/Models/Category.php (filled: Fillable, casts, parent/children/products relations)
- app/Models/Brand.php (filled: Fillable, casts, products relation)
- app/Models/Product.php (filled: Fillable, array casts for images+technical_specs, category/brand relations)
- public/storage symlink
</output>
