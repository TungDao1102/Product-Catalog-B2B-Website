---
phase: 03
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/Filament/Resources/Inquiries/InquiryResource.php
  - app/Filament/Resources/Inquiries/Schemas/InquiryForm.php
  - app/Filament/Resources/Inquiries/Tables/InquiriesTable.php
  - app/Filament/Resources/Inquiries/Pages/ListInquiries.php
  - app/Filament/Resources/Inquiries/Pages/ViewInquiry.php
  - app/Filament/Resources/Contacts/ContactResource.php
  - app/Filament/Resources/Contacts/Schemas/ContactForm.php
  - app/Filament/Resources/Contacts/Tables/ContactsTable.php
  - app/Filament/Resources/Contacts/Pages/ListContacts.php
  - app/Filament/Resources/Contacts/Pages/ViewContact.php
autonomous: true
requirements:
  - REQ-03
  - REQ-04
user_setup: []

must_haves:
  truths:
    - "Admin can view a list of all Inquiries (quote requests) in Filament admin panel"
    - "Admin can view Inquiry detail with all form fields displayed read-only"
    - "Admin can mark Inquiry as read (is_read updated when viewed)"
    - "Admin can delete Inquiries"
    - "Admin cannot edit Inquiry data (no EditRecord page) per D-14"
    - "Admin can view a list of Contacts in Filament admin panel"
    - "Admin can view Contact detail with all fields displayed read-only"
    - "Admin can mark Contact as read (is_read updated when viewed)"
    - "Admin can delete Contacts"
    - "InquiryResource appears in sidebar at navigationSort=6"
    - "ContactResource appears in sidebar at navigationSort=7"
  artifacts:
    - path: "app/Filament/Resources/Inquiries/InquiryResource.php"
      provides: "Inquiry Filament Resource with read-only ViewRecord page and DeleteAction"
      contains: "navigationSort = 6"
    - path: "app/Filament/Resources/Inquiries/Schemas/InquiryForm.php"
      provides: "Inquiry form schema with read-only display fields (name, email, phone, company, quantity, message, product)"
      contains: "TextInput.*readOnly\|Placeholder"
    - path: "app/Filament/Resources/Inquiries/Tables/InquiriesTable.php"
      provides: "Inquiry table with columns for name, email, is_read (IconColumn), product, created_at"
      contains: "IconColumn.*boolean"
    - path: "app/Filament/Resources/Inquiries/Pages/ListInquiries.php"
      provides: "Inquiry list page"
      exports: ["ListInquiries"]
    - path: "app/Filament/Resources/Inquiries/Pages/ViewInquiry.php"
      provides: "Inquiry view page with DeleteAction header action, marks is_read on view"
      contains: "ViewRecord\|DeleteAction"
    - path: "app/Filament/Resources/Contacts/ContactResource.php"
      provides: "Contact Filament Resource with read-only ViewRecord page and DeleteAction"
      contains: "navigationSort = 7"
    - path: "app/Filament/Resources/Contacts/Schemas/ContactForm.php"
      provides: "Contact form schema with read-only display fields"
      contains: "TextInput.*readOnly\|Placeholder"
    - path: "app/Filament/Resources/Contacts/Tables/ContactsTable.php"
      provides: "Contact table with columns for name, email, is_read (IconColumn), created_at"
      contains: "IconColumn"
    - path: "app/Filament/Resources/Contacts/Pages/ViewContact.php"
      provides: "Contact view page with DeleteAction, marks is_read on view"
      contains: "ViewRecord\|DeleteAction"
  key_links:
    - from: "app/Filament/Resources/Inquiries/InquiryResource.php"
      to: "app/Filament/Resources/Inquiries/Pages/ViewInquiry.php"
      via: "getPages() returns view page"
      pattern: "'view'.*ViewInquiry"
    - from: "app/Filament/Resources/Inquiries/InquiryResource.php"
      to: "app/Models/Inquiry.php"
      via: "$model = Inquiry::class"
      pattern: "model.*Inquiry"
    - from: "app/Filament/Resources/Inquiries/Pages/ViewInquiry.php"
      to: "app/Models/Inquiry.php"
      via: "mutateFormDataBeforeFill marks is_read as true"
      pattern: "is_read.*true"
    - from: "app/Filament/Resources/Contacts/ContactResource.php"
      to: "app/Models/Contact.php"
      via: "$model = Contact::class"
      pattern: "model.*Contact"
    - from: "app/Filament/Resources/Contacts/Pages/ViewContact.php"
      to: "app/Models/Contact.php"
      via: "mutateFormDataBeforeFill marks is_read as true"
      pattern: "is_read"
---

<objective>
**Phase 3, Plan 03:** Inquiry & Contact Filament Resources (Read-only management).

**Purpose:** Create two read-only Filament Resources for managing customer-submitted inquiries (quote requests) and contacts. Per D-14, these are view-only — admin can view details, mark as read (automatic on view), and delete, but cannot edit customer submissions.

**Output:** 10 files: 2 Resources, 2 Form schemas (read-only), 2 Table configs, 2 List pages, 2 View pages.
</objective>

<execution_context>
@C:/Users/daotu/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/daotu/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03/03-CONTEXT.md
@.planning/phases/03/03-RESEARCH.md

@app/Filament/Resources/Products/ProductResource.php
@app/Filament/Resources/Brands/BrandResource.php
@app/Models/Inquiry.php
@app/Models/Contact.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InquiryResource with read-only view and delete capability</name>
  <files>
    app/Filament/Resources/Inquiries/InquiryResource.php,
    app/Filament/Resources/Inquiries/Schemas/InquiryForm.php,
    app/Filament/Resources/Inquiries/Tables/InquiriesTable.php,
    app/Filament/Resources/Inquiries/Pages/ListInquiries.php,
    app/Filament/Resources/Inquiries/Pages/ViewInquiry.php
  </files>
  <read_first>
    @.planning/phases/03/03-RESEARCH.md (Inquiry Resource read-only pattern)
    @app/Models/Inquiry.php (model with fillable, product relationship)
  </read_first>
  <action>
    Create the Inquiries resource directory under `app/Filament/Resources/Inquiries/` with 5 files. Per D-14: read-only — ListRecords + ViewRecord (not EditRecord), ViewRecord marks is_read, DeleteAction in header.

    **InquiryResource.php** (Namespace: `App\Filament\Resources\Inquiries`):
    - `$model = Inquiry::class`, `$navigationIcon = 'heroicon-o-chat-bubble-left-right'`, `$navigationSort = 6` (per D-13)
    - `$navigationLabel = 'Yêu cầu báo giá'`, `$modelLabel = 'Yêu cầu báo giá'`, `$pluralModelLabel = 'Yêu cầu báo giá'`
    - `$recordTitleAttribute = 'name'`
    - `form(Schema $schema)`: delegates to `InquiryForm::configure($schema)`
    - `table(Table $table)`: delegates to `InquiriesTable::configure($table)`
    - `getPages()`: only 'index' → ListInquiries and 'view' → ViewInquiry (NOT 'create' or 'edit')

    **InquiryForm.php** (Namespace: `App\Filament\Resources\Inquiries\Schemas`):
    - All fields displayed as read-only (disabled/placeholder) — admin should not change customer data per D-14
    - Field display approach: Use `TextInput::make(...)->disabled()` or use `Placeholder` / `Section` with Infolist-style display. Recommended: `TextInput` with `->disabled()` and `->dehydrated(false)` for each field so they display the stored value but cannot be changed:
      - `name`, `email`, `phone`, `company`, `quantity` (numeric), `message` (Textarea → disabled)
      - `product_id`: Use `Select::make('product_id')->relationship('product', 'name')->disabled()->dehydrated(false)` to show product name
      - `is_read`: Toggle or TextInput showing current status
      - `created_at`: TextInput showing submission time

    **InquiriesTable.php** (Namespace: `App\Filament\Resources\Inquiries\Tables`):
    - Columns: TextColumn 'name' (searchable), TextColumn 'email', TextColumn 'phone', TextColumn 'company', TextColumn 'product.name' (label 'Sản phẩm'), IconColumn 'is_read' (boolean, label 'Đã đọc'), TextColumn 'created_at' (dateTime, sortable, label 'Ngày gửi')
    - Filters: TernaryFilter for 'is_read' (label 'Đã đọc')
    - DefaultSort: 'created_at' desc (newest first)
    - Actions: ViewAction (instead of EditAction), DeleteAction
    - BulkActionGroup: DeleteBulkAction

    **ListInquiries.php** (Namespace: `App\Filament\Resources\Inquiries\Pages`):
    - Extends `ListRecords`, `$resource = InquiryResource::class`
    - NO CreateAction in header — admins do not create inquiries manually

    **ViewInquiry.php** (Namespace: `App\Filament\Resources\Inquiries\Pages`):
    - Extends `ViewRecord`, `$resource = InquiryResource::class`
    - `getHeaderActions()` returns `[DeleteAction::make()]` only (no EditAction)
    - `mutateFormDataBeforeFill(array $data): array`:
      - Call `$this->getRecord()->update(['is_read' => true])` to auto-mark as read when admin views it
      - Return `$data`
  </action>
  <verify>
    <automated>php artisan tinker --execute="new App\Filament\Resources\Inquiries\InquiryResource" 2>&1</automated>
  </verify>
  <done>InquiryResource created with 5 files. Read-only forms. is_read auto-marks on view. ViewRecord with DeleteAction. Navigation sort = 6.</done>
</task>

<task type="auto">
  <name>Task 2: Create ContactResource with read-only view and delete capability</name>
  <files>
    app/Filament/Resources/Contacts/ContactResource.php,
    app/Filament/Resources/Contacts/Schemas/ContactForm.php,
    app/Filament/Resources/Contacts/Tables/ContactsTable.php,
    app/Filament/Resources/Contacts/Pages/ListContacts.php,
    app/Filament/Resources/Contacts/Pages/ViewContact.php
  </files>
  <read_first>
    @.planning/phases/03/03-RESEARCH.md (Inquiry/Contact read-only pattern — same structure)
    @app/Models/Contact.php (model)
  </read_first>
  <action>
    Create the Contacts resource directory under `app/Filament/Resources/Contacts/` with 5 files, following the same read-only pattern as InquiryResource.

    **ContactResource.php** (Namespace: `App\Filament\Resources\Contacts`):
    - `$model = Contact::class`, `$navigationIcon = 'heroicon-o-envelope'`, `$navigationSort = 7` (per D-13)
    - `$navigationLabel = 'Liên hệ'`, `$modelLabel = 'Liên hệ'`, `$pluralModelLabel = 'Liên hệ'`
    - `$recordTitleAttribute = 'name'`
    - Delegates form/table to ContactForm/ContactsTable
    - `getPages()`: index → ListContacts, view → ViewContact (no create or edit)

    **ContactForm.php** (Namespace: `App\Filament\Resources\Contacts\Schemas`):
    - All fields disabled and dehydrated(false): name, email, phone, company, message (Textarea), is_read, created_at

    **ContactsTable.php** (Namespace: `App\Filament\Resources\Contacts\Tables`):
    - Columns: TextColumn 'name' (searchable), TextColumn 'email', TextColumn 'phone', TextColumn 'company', IconColumn 'is_read' (boolean, label 'Đã đọc'), TextColumn 'created_at' (dateTime, sortable, label 'Ngày gửi')
    - Filters: TernaryFilter for 'is_read'
    - DefaultSort: 'created_at' desc
    - Actions: ViewAction, DeleteAction
    - BulkActionGroup: DeleteBulkAction

    **ListContacts.php**: extends ListRecords, $resource = ContactResource, no CreateAction header
    **ViewContact.php**: extends ViewRecord, $resource = ContactResource
    - `getHeaderActions()` returns `[DeleteAction::make()]`
    - `mutateFormDataBeforeFill` marks is_read = true when admin views
  </action>
  <verify>
    <automated>php artisan tinker --execute="new App\Filament\Resources\Contacts\ContactResource" 2>&1</automated>
  </verify>
  <done>ContactResource created with 5 files. Same read-only pattern as InquiryResource. Navigation sort = 7.</done>
</task>

<task type="auto">
  <name>Task 3: Create InquiryResourceTest and ContactResourceTest</name>
  <files>
    tests/Feature/Filament/InquiryResourceTest.php,
    tests/Feature/Filament/ContactResourceTest.php
  </files>
  <read_first>
    @tests/Feature/Filament/CategoryResourceTest.php (pattern)
  </read_first>
  <action>
    Per Nyquist rule, create 2 test files covering REQ-03 and REQ-04:

    **InquiryResourceTest.php**:
    - Namespace `Tests\Feature\Filament`, extends `TestCase`
    - `test_can_list_inquiries()`: Login as admin, get index page → 200
    - `test_can_view_inquiry()`: Create Inquiry via model, navigate to view page → 200, page contains inquiry data
    - `test_mark_as_read_on_view()`: Create inquiry with is_read=false, visit view page → assert is_read becomes true in DB
    - `test_can_delete_inquiry()`: Delete via action → DB record removed
    - Seed an inquiry in setUp if needed, or use `Inquiry::factory()` — if no factory exists, use direct `Inquiry::create()`

    **ContactResourceTest.php**:
    - Same structure as InquiryResourceTest
    - `test_can_list_contacts()`, `test_can_view_contact()`, `test_mark_as_read_on_view()`, `test_can_delete_contact()`
  </action>
  <verify>
    <automated>php artisan test --filter=InquiryResourceTest --filter=ContactResourceTest 2>&1</automated>
  </verify>
  <done>2 test files created. InquiryResourceTest and ContactResourceTest cover list, view, mark-as-read, and delete operations. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → Inquiry/Contact Resource | Authenticated admin viewing customer-submitted data |
| Customer → Form Submission → Database | Untrusted data enters system via public forms (covered in Plan 05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Inquiry/Contact Resource | mitigate | Admin authentication required (Filament panel gate); no public exposure of customer data |
| T-03-06 | Tampering | is_read field | accept | Mark-as-read on view is intentional behavior; no manual is_read editing available |
</threat_model>

<verification>
1. InquiryResource loads in sidebar at position 6: navigate to `/admin` → sidebar shows "Yêu cầu báo giá"
2. ContactResource loads in sidebar at position 7: sidebar shows "Liên hệ"
3. Create an Inquiry via tinker: `App\Models\Inquiry::create([...])` → it appears in the list
4. View an Inquiry detail → is_read auto-updates to true
5. Delete action removes the record
6. Same verification for ContactResource
7. `php artisan test --filter=InquiryResourceTest --filter=ContactResourceTest` passes
</verification>

<success_criteria>
- InquiryResource and ContactResource created with read-only ViewRecord pattern
- All form fields displayed as disabled (customer data preserved per D-14)
- is_read auto-marks to true when admin views detail via mutateFormDataBeforeFill
- DeleteAction available in ViewRecord header
- Navigation sort: Inquiry=6, Contact=7 (per D-13)
- No create/edit pages — ListRecords + ViewRecord only
- CRUD tests pass for both resources
</success_criteria>

<output>
Create `.planning/phases/03/03-03-SUMMARY.md` when done.
</output>
