---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Models/Post.php
  - app/Models/Project.php
  - app/Models/Contact.php
  - app/Models/Inquiry.php
  - app/Mail/AdminNotification.php
  - app/Mail/CustomerConfirmation.php
  - resources/views/mail/admin-notification.blade.php
  - resources/views/mail/customer-confirmation.blade.php
  - database/seeders/PostSeeder.php
  - database/seeders/ProjectSeeder.php
  - database/seeders/DatabaseSeeder.php
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-09
  - REQ-10
  - REQ-11
user_setup: []

must_haves:
  truths:
    - "Post, Project, Contact, Inquiry models have #[Fillable] attributes matching their DB columns"
    - "Inquiry model has product() BelongsTo relationship"
    - "Project model casts images JSON column as array"
    - "AdminNotification and CustomerConfirmation Mailable classes exist with envelope() and content()"
    - "Two email Blade templates exist at resources/views/mail/ with submission details"
    - "PostSeeder creates 3-5 published posts with all required fields"
    - "ProjectSeeder creates 3-5 active projects with JSON image arrays"
    - "DatabaseSeeder calls PostSeeder and ProjectSeeder"
    - "php artisan db:seed runs without error, creating all seed data"
  artifacts:
    - path: "app/Models/Post.php"
      provides: "Post model with #[Fillable] and casts"
      contains: "Fillable|casts"
    - path: "app/Models/Project.php"
      provides: "Project model with #[Fillable], images array cast, is_active boolean cast"
      contains: "casts.*array"
    - path: "app/Models/Contact.php"
      provides: "Contact model with #[Fillable] and is_read boolean cast"
      contains: "Fillable"
    - path: "app/Models/Inquiry.php"
      provides: "Inquiry model with #[Fillable], product() BelongsTo, casts"
      contains: "belongsTo.*Product"
    - path: "app/Mail/AdminNotification.php"
      provides: "Admin notification Mailable with envelope() and content()"
      exports: ["AdminNotification"]
    - path: "app/Mail/CustomerConfirmation.php"
      provides: "Customer confirmation Mailable with envelope() and content()"
      exports: ["CustomerConfirmation"]
    - path: "resources/views/mail/admin-notification.blade.php"
      provides: "Email template for admin notification with submission details"
      min_lines: 20
    - path: "resources/views/mail/customer-confirmation.blade.php"
      provides: "Email template for customer confirmation"
      min_lines: 15
    - path: "database/seeders/PostSeeder.php"
      provides: "Seeder creating 3-5 posts with title, slug, excerpt, content, image, published_at"
      contains: "create"
    - path: "database/seeders/ProjectSeeder.php"
      provides: "Seeder creating 3-5 projects with title, slug, description, content, images, is_active"
      contains: "create"
  key_links:
    - from: "app/Models/Inquiry.php"
      to: "app/Models/Product.php"
      via: "belongsTo relationship"
      pattern: "belongsTo"
    - from: "app/Mail/AdminNotification.php"
      to: "resources/views/mail/admin-notification.blade.php"
      via: "Content view reference"
      pattern: "view.*admin-notification"
    - from: "app/Mail/CustomerConfirmation.php"
      to: "resources/views/mail/customer-confirmation.blade.php"
      via: "Content view reference"
      pattern: "view.*customer-confirmation"
    - from: "database/seeders/PostSeeder.php"
      to: "app/Models/Post.php"
      via: "Post::create() calls"
      pattern: "Post::"
    - from: "database/seeders/ProjectSeeder.php"
      to: "app/Models/Project.php"
      via: "Project::create() calls"
      pattern: "Project::"
---

<objective>
**Phase 3, Plan 01:** Foundation — Models, Mailables, Seeders.

**Purpose:** Establish the data layer and email infrastructure for Phase 3. All 4 models (Post, Project, Contact, Inquiry) need fillable attributes, type casts, and relationships defined. Two Mailable classes with Blade email templates enable admin notification and customer confirmation emails. Seeders create sample data for posts and projects.

**Output:** 4 populated Eloquent models, 2 Mailable classes, 2 email Blade templates, 2 seeders + DatabaseSeeder update.
</objective>

<execution_context>
@C:/Users/daotu/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/daotu/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03/03-CONTEXT.md
@.planning/phases/03/03-RESEARCH.md

@database/migrations/2026_06_20_165917_create_posts_table.php
@database/migrations/2026_06_20_165918_create_projects_table.php
@database/migrations/2026_06_20_165918_create_contacts_table.php
@database/migrations/2026_06_20_165919_create_inquiries_table.php
@database/seeders/DatabaseSeeder.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 4 models with #[Fillable] attributes, casts, and relationships</name>
  <files>
    app/Models/Post.php,
    app/Models/Project.php,
    app/Models/Contact.php,
    app/Models/Inquiry.php
  </files>
  <read_first>
    @database/migrations/2026_06_20_165917_create_posts_table.php
    @database/migrations/2026_06_20_165918_create_projects_table.php
    @database/migrations/2026_06_20_165918_create_contacts_table.php
    @database/migrations/2026_06_20_165919_create_inquiries_table.php
    @app/Models/Category.php (for #[Fillable] attribute convention reference)
  </read_first>
  <action>
    Using PHP 8.4 attribute syntax with `Illuminate\Database\Eloquent\Attributes\Fillable`, update all 4 model stubs (currently empty):

    **Post.php** — per D-03:
    - `#[Fillable(['title', 'slug', 'excerpt', 'content', 'image', 'is_published', 'published_at'])]`
    - `casts()` returning `['is_published' => 'boolean', 'published_at' => 'datetime']`

    **Project.php** — per D-04, D-05:
    - `#[Fillable(['title', 'slug', 'description', 'content', 'images', 'is_active'])]`
    - `casts()` returning `['images' => 'array', 'is_active' => 'boolean']`

    **Contact.php** — per D-09, D-11:
    - `#[Fillable(['name', 'email', 'phone', 'company', 'message', 'is_read'])]`
    - `casts()` returning `['is_read' => 'boolean']`

    **Inquiry.php** — per D-07, D-08:
    - `#[Fillable(['product_id', 'name', 'email', 'phone', 'company', 'quantity', 'message', 'is_read'])]`
    - `casts()` returning `['is_read' => 'boolean', 'quantity' => 'integer']`
    - `product(): BelongsTo` relationship returning `$this->belongsTo(Product::class)`
    - Import `Illuminate\Database\Eloquent\Relations\BelongsTo`

    Do NOT use `$fillable` property — the project convention uses PHP 8.4 `#[Fillable]` attribute per existing Category/Brand/Product models.
  </action>
  <verify>
    <automated>php artisan tinker --execute="(new App\Models\Post)->getFillable();" 2>&1 && php artisan tinker --execute="(new App\Models\Project)->getFillable();" 2>&1 && php artisan tinker --execute="(new App\Models\Inquiry)->product();" 2>&1</automated>
  </verify>
  <done>All 4 models have #[Fillable] attributes matching DB columns. Inquiry has product() relationship. Project casts images as array.</done>
</task>

<task type="auto">
  <name>Task 2: Create 2 Mailable classes and 2 email Blade templates</name>
  <files>
    app/Mail/AdminNotification.php,
    app/Mail/CustomerConfirmation.php,
    resources/views/mail/admin-notification.blade.php,
    resources/views/mail/customer-confirmation.blade.php
  </files>
  <read_first>
    @config/mail.php (for MAIL_FROM_ADDRESS/MAIL_FROM_NAME)
    @.planning/phases/03/03-RESEARCH.md (Mailable class patterns)
  </read_first>
  <action>
    Per D-15, D-16, D-17, D-18.

    **AdminNotification.php:**
    - Namespace `App\Mail`, use `Illuminate\Bus\Queueable`, `Illuminate\Mail\Mailable`, `Illuminate\Mail\Mailables\Content`, `Illuminate\Mail\Mailables\Envelope`
    - Constructor accepts `public Inquiry|Contact $submission` and `public string $type` ('inquiry' | 'contact')
    - `envelope()`: subject = match type — 'inquiry' => "Yêu cầu báo giá mới từ {$name}", 'contact' => "Liên hệ mới từ {$name}"
    - `content()`: view = 'mail.admin-notification'
    - Do NOT use Queueable's queue methods — synchronous only per D-16

    **CustomerConfirmation.php:**
    - Same structure, accepts `Inquiry|Contact $submission` and `string $type`
    - Subject: 'inquiry' => "Xác nhận đã nhận yêu cầu báo giá", 'contact' => "Xác nhận đã nhận liên hệ của bạn"
    - View = 'mail.customer-confirmation'

    **admin-notification.blade.php:**
    - Simple HTML email with inline styles (email client compatibility)
    - Header: "Yêu cầu báo giá mới" or "Liên hệ mới" depending on `$type`
    - Table showing: name, email, phone, company, and (if type='inquiry') product name, quantity
    - Message body section
    - Footer: "Email này được gửi tự động từ hệ thống quản lý website."
    - Access submission fields via `$submission->name`, `$submission->email`, etc.
    - For product name: `$submission->product->name ?? '—'` (only for inquiry type)

    **customer-confirmation.blade.php:**
    - Header thanking the customer
    - Content: "Chúng tôi đã nhận được yêu cầu của bạn và sẽ liên hệ lại trong thời gian sớm nhất."
    - Summary table showing submitted info (name, email, phone, company, message)
    - If type='inquiry': include product and quantity info
    - Footer with company name
  </action>
  <verify>
    <automated>php artisan tinker --execute="new App\Mail\AdminNotification(new App\Models\Contact(['name'=>'Test','email'=>'t@t.com','message'=>'Hi']), 'contact');" 2>&1</automated>
  </verify>
  <done>AdminNotification and CustomerConfirmation Mailable classes created. Two Blade email templates at resources/views/mail/. Mailables construct without errors and reference correct views.</done>
</task>

<task type="auto">
  <name>Task 3: Create PostSeeder, ProjectSeeder with sample data, update DatabaseSeeder</name>
  <files>
    database/seeders/PostSeeder.php,
    database/seeders/ProjectSeeder.php,
    database/seeders/DatabaseSeeder.php
  </files>
  <read_first>
    @database/seeders/DatabaseSeeder.php
    @app/Models/Category.php (for ::create pattern reference)
  </read_first>
  <action>
    Per D-19, D-20, D-21.

    **PostSeeder.php:**
    - Use `WithoutModelEvents` trait
    - Create 3-5 posts with Vietnamese sample content:
      - title, slug (use `Str::slug($title)`), excerpt (2-3 sentences), content (HTML paragraphs), image ('posts/sample-1.jpg' etc. — placeholder paths matching existing public disk structure), is_published (true), published_at (now() subtract random days)
    - Topics: general company news, product category highlights, industry insights
    - Do NOT use factories — direct `Post::create()` with hardcoded arrays (existing project pattern)

    **ProjectSeeder.php:**
    - Create 3-5 projects with Vietnamese sample titles:
      - title, slug, description (brief), content (HTML paragraphs), images (array of 2-3 placeholder paths like 'projects/sample-1.jpg'), is_active (true)
    - Topics: typical installation/system integration projects

    **DatabaseSeeder.php update:**
    - Add `PostSeeder::class` and `ProjectSeeder::class` to the `$this->call([...])` array after existing seeders

    Create `storage/app/public/posts/` and `storage/app/public/projects/` directories if they don't exist (mkdir -p equivalent). Verify `php artisan storage:link` is active — if not, create a checkpoint for the user.
  </action>
  <verify>
    <automated>php artisan db:seed --class=DatabaseSeeder 2>&1</automated>
  </verify>
  <done>PostSeeder and ProjectSeeder create 3-5 records each. DatabaseSeeder calls both. `db:seed` completes with 0 errors. Storage directories created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seeder → Database | Seed data written during `db:seed` — trusted admin operation |
| Mailable → Mailer | Email content rendered from trusted admin content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Post/Project seed data | accept | Seed data is trusted sample content for development only |
| T-03-02 | Information Disclosure | Email templates | accept | Email templates only display data the user submitted; no PII beyond what user entered |
</threat_model>

<verification>
1. All 4 models load without errors: `php artisan tinker --execute="(new App\Models\Post)->getTable();"`
2. Inquiry::product() relationship resolves: `php artisan tinker --execute="App\Models\Inquiry::first()?->product;"`
3. Mailables instantiate: `php artisan tinker --execute="new App\Mail\AdminNotification(new App\Models\Inquiry(['name'=>'A','email'=>'a@a.com','message'=>'M']), 'inquiry');"`
4. Seeders run: `php artisan db:seed`
5. `php artisan test --filter=SeederTest` passes once scaffold test is written
</verification>

<success_criteria>
- All model stubs replaced with full #[Fillable], casts, and relationships
- AdminNotification and CustomerConfirmation mailables + Blade templates created
- PostSeeder and ProjectSeeder populate sample data
- DatabaseSeeder calls new seeders
- `php artisan db:seed` completes successfully
</success_criteria>

<output>
Create `.planning/phases/03/03-01-SUMMARY.md` when done.
</output>
