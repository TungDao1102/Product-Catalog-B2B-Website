---
phase: 03
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/Filament/Resources/Posts/PostResource.php
  - app/Filament/Resources/Posts/Schemas/PostForm.php
  - app/Filament/Resources/Posts/Tables/PostsTable.php
  - app/Filament/Resources/Posts/Pages/ListPosts.php
  - app/Filament/Resources/Posts/Pages/CreatePost.php
  - app/Filament/Resources/Posts/Pages/EditPost.php
  - app/Filament/Resources/Projects/ProjectResource.php
  - app/Filament/Resources/Projects/Schemas/ProjectForm.php
  - app/Filament/Resources/Projects/Tables/ProjectsTable.php
  - app/Filament/Resources/Projects/Pages/ListProjects.php
  - app/Filament/Resources/Projects/Pages/CreateProject.php
  - app/Filament/Resources/Projects/Pages/EditProject.php
autonomous: true
requirements:
  - REQ-01
  - REQ-02
user_setup: []

must_haves:
  truths:
    - "Admin can create, read, update, and delete Posts via Filament admin panel"
    - "Post form has RichEditor for content with image upload capability per D-02"
    - "Admin can create, read, update, and delete Projects via Filament admin panel"
    - "Project form has FileUpload multiple with reorderable for images gallery per D-04"
    - "PostResource appears in sidebar at navigationSort=4 with heroicon-o-newspaper"
    - "ProjectResource appears in sidebar at navigationSort=5 with heroicon-o-folder-open"
    - "Both resources follow modular pattern: Schema class + Table class + separate Page classes"
    - "Post slug auto-generates from title on create"
    - "Project slug auto-generates from title on create"
  artifacts:
    - path: "app/Filament/Resources/Posts/PostResource.php"
      provides: "Post Filament Resource with form() delegating to PostForm, table() to PostsTable"
      contains: "PostForm::configure"
    - path: "app/Filament/Resources/Posts/Schemas/PostForm.php"
      provides: "Post form schema with title, slug, excerpt, RichEditor content, image upload, is_published toggle, published_at picker"
      contains: "RichEditor::make"
    - path: "app/Filament/Resources/Posts/Tables/PostsTable.php"
      provides: "Post table with columns for title, is_published (ToggleColumn), published_at"
      contains: "ToggleColumn"
    - path: "app/Filament/Resources/Posts/Pages/ListPosts.php"
      provides: "Post list page with CreateAction header"
      exports: ["ListPosts"]
    - path: "app/Filament/Resources/Posts/Pages/CreatePost.php"
      provides: "Post create page"
      exports: ["CreatePost"]
    - path: "app/Filament/Resources/Posts/Pages/EditPost.php"
      provides: "Post edit page"
      exports: ["EditPost"]
    - path: "app/Filament/Resources/Projects/ProjectResource.php"
      provides: "Project Filament Resource delegating to ProjectForm and ProjectsTable"
      contains: "ProjectForm::configure"
    - path: "app/Filament/Resources/Projects/Schemas/ProjectForm.php"
      provides: "Project form schema with title, slug, description, RichEditor content, FileUpload multiple for images, is_active toggle"
      contains: "FileUpload::make.*multiple"
    - path: "app/Filament/Resources/Projects/Tables/ProjectsTable.php"
      provides: "Project table with columns for title, is_active (ToggleColumn)"
      contains: "ToggleColumn"
    - path: "app/Filament/Resources/Projects/Pages/CreateProject.php"
      provides: "Project create page"
      exports: ["CreateProject"]
    - path: "app/Filament/Resources/Projects/Pages/EditProject.php"
      provides: "Project edit page"
      exports: ["EditProject"]
  key_links:
    - from: "app/Filament/Resources/Posts/PostResource.php"
      to: "app/Filament/Resources/Posts/Schemas/PostForm.php"
      via: "PostForm::configure call"
      pattern: "PostForm::configure"
    - from: "app/Filament/Resources/Posts/PostResource.php"
      to: "app/Filament/Resources/Posts/Tables/PostsTable.php"
      via: "PostsTable::configure call"
      pattern: "PostsTable::configure"
    - from: "app/Filament/Resources/Posts/PostResource.php"
      to: "app/Models/Post.php"
      via: "$model = Post::class"
      pattern: "model.*Post::class"
    - from: "app/Filament/Resources/Projects/ProjectResource.php"
      to: "app/Filament/Resources/Projects/Schemas/ProjectForm.php"
      via: "ProjectForm::configure call"
      pattern: "ProjectForm::configure"
    - from: "app/Filament/Resources/Projects/Schemas/ProjectForm.php"
      to: "app/Models/Project.php"
      via: "FileUpload stores JSON to images attribute"
      pattern: "images"
---

<objective>
**Phase 3, Plan 02:** Post & Project Filament Resources (Full CRUD).

**Purpose:** Create two full CRUD Filament Resources for managing Posts (news/blog) and Projects (case studies) following the modular pattern established in Phase 2 (Resource → Schema/Form class → Table class → Page classes).

**Output:** 12 files: 2 Resources, 2 Form schemas, 2 Table configs, 2 List pages, 2 Create pages, 2 Edit pages.
</objective>

<execution_context>
@C:/Users/daotu/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/daotu/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03/03-CONTEXT.md
@.planning/phases/03/03-RESEARCH.md

@app/Filament/Resources/Products/ProductResource.php
@app/Filament/Resources/Products/Schemas/ProductForm.php
@app/Filament/Resources/Products/Tables/ProductsTable.php
@app/Filament/Resources/Products/Pages/CreateProduct.php
@app/Filament/Resources/Brands/BrandResource.php
@app/Filament/Resources/Brands/Pages/CreateBrand.php
@app/Models/Post.php
@app/Models/Project.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostResource with modular Form schema, Table config, and Pages</name>
  <files>
    app/Filament/Resources/Posts/PostResource.php,
    app/Filament/Resources/Posts/Schemas/PostForm.php,
    app/Filament/Resources/Posts/Tables/PostsTable.php,
    app/Filament/Resources/Posts/Pages/ListPosts.php,
    app/Filament/Resources/Posts/Pages/CreatePost.php,
    app/Filament/Resources/Posts/Pages/EditPost.php
  </files>
  <read_first>
    @app/Filament/Resources/Products/ProductResource.php (pattern for Resource class)
    @app/Filament/Resources/Products/Schemas/ProductForm.php (pattern for Form schema)
    @app/Filament/Resources/Products/Tables/ProductsTable.php (pattern for Table)
    @app/Filament/Resources/Products/Pages/CreateProduct.php (pattern for Create page with afterSave)
    @app/Models/Post.php (model with fillable)
  </read_first>
  <action>
    Create the Posts resource directory under `app/Filament/Resources/Posts/` with 6 files, precisely following the Phase 2 modular pattern:

    **PostResource.php** (Namespace: `App\Filament\Resources\Posts`):
    - `$model = Post::class`, `$navigationIcon = 'heroicon-o-newspaper'`, `$navigationSort = 4` (per D-13)
    - `$navigationLabel = 'Tin tức'`, `$modelLabel = 'Bài viết'`, `$pluralModelLabel = 'Tin tức'`
    - `$recordTitleAttribute = 'title'`
    - `form(Schema $schema): Schema` returns `PostForm::configure($schema)`
    - `table(Table $table): Table` returns `PostsTable::configure($table)`
    - `getPages()` returns index → ListPosts, create → CreatePost, edit → EditPost
    - No `getRelations()` needed

    **PostForm.php** (Namespace: `App\Filament\Resources\Posts\Schemas`):
    Per D-02 (RichEditor) and D-03 (fields):
    ```php
    class PostForm
    {
        public static function configure(Schema $schema): Schema
        {
            return $schema->components([
                Section::make('Nội dung bài viết')->schema([
                    TextInput::make('title')->required()->live(true)
                        ->afterStateUpdated(fn(string $op, $state, Set $set) => $op === 'create' ? $set('slug', Str::slug($state)) : null),
                    TextInput::make('slug')->required()->unique(ignoreRecord: true),
                    Textarea::make('excerpt')->label('Mô tả ngắn')->columnSpanFull(),
                    RichEditor::make('content')->label('Nội dung')
                        ->fileAttachmentsDisk('public')->fileAttachmentsDirectory('posts')->columnSpanFull()
                        ->toolbarButtons(['bold','italic','underline','strike','h2','h3','bulletList','orderedList','blockquote','link','image','undo','redo']),
                    FileUpload::make('image')->image()->disk('public')->directory('posts/thumbnails')->visibility('public')->label('Ảnh đại diện'),
                    Toggle::make('is_published')->label('Đã xuất bản'),
                    DateTimePicker::make('published_at')->label('Ngày xuất bản'),
                ]),
            ]);
        }
    }
    ```
    Import `Str`, `Set`, `Get` from their Filament/Framework namespaces.

    **PostsTable.php** (Namespace: `App\Filament\Resources\Posts\Tables`):
    - Columns: TextColumn 'title' (searchable, sortable, limit 50), TextColumn 'excerpt' (limit 60), ToggleColumn 'is_published' (label 'Đã xuất bản'), TextColumn 'published_at' (dateTime, sortable)
    - Filters: TernaryFilter for 'is_published'
    - DefaultSort: 'published_at' desc
    - Actions: EditAction, DeleteAction
    - BulkActionGroup: DeleteBulkAction

    **ListPosts.php** (Namespace: `App\Filament\Resources\Posts\Pages`):
    - Extends `ListRecords`, `$resource = PostResource::class`
    - `getHeaderActions()` returns `[CreateAction::make()]`

    **CreatePost.php**:
    - Extends `CreateRecord`, `$resource = PostResource::class`
    - No afterSave needed (posts don't need image resize per Research — agent discretion)

    **EditPost.php**:
    - Extends `EditRecord`, `$resource = PostResource::class`
    - No afterSave needed
  </action>
  <verify>
    <automated>php artisan tinker --execute="new App\Filament\Resources\Posts\PostResource" 2>&1</automated>
  </verify>
  <done>PostResource with 6 modular files created. PostForm includes RichEditor with image attachment support. Navigation sort = 4. Resource instantiates without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProjectResource with modular Form schema (images gallery), Table config, and Pages</name>
  <files>
    app/Filament/Resources/Projects/ProjectResource.php,
    app/Filament/Resources/Projects/Schemas/ProjectForm.php,
    app/Filament/Resources/Projects/Tables/ProjectsTable.php,
    app/Filament/Resources/Projects/Pages/ListProjects.php,
    app/Filament/Resources/Projects/Pages/CreateProject.php,
    app/Filament/Resources/Projects/Pages/EditProject.php
  </files>
  <read_first>
    @app/Filament/Resources/Products/ProductResource.php (pattern)
    @app/Filament/Resources/Products/Schemas/ProductForm.php (FileUpload multiple pattern)
    @app/Models/Project.php (model with images array cast)
  </read_first>
  <action>
    Create the Projects resource directory under `app/Filament/Resources/Projects/` with 6 files:

    **ProjectResource.php** (Namespace: `App\Filament\Resources\Projects`):
    - `$model = Project::class`, `$navigationIcon = 'heroicon-o-folder-open'`, `$navigationSort = 5` (per D-13)
    - `$navigationLabel = 'Dự án'`, `$modelLabel = 'Dự án'`, `$pluralModelLabel = 'Dự án'`
    - `$recordTitleAttribute = 'title'`
    - Delegates form() to ProjectForm, table() to ProjectsTable
    - getPages(): index→ListProjects, create→CreateProject, edit→EditProject

    **ProjectForm.php** (Namespace: `App\Filament\Resources\Projects\Schemas`):
    Per D-04 (JSON images gallery) and D-05 (fields):
    ```php
    Section::make('Nội dung dự án')->schema([
        TextInput::make('title')->required()->live(true)
            ->afterStateUpdated(fn(string $op, $state, Set $set) => $op === 'create' ? $set('slug', Str::slug($state)) : null),
        TextInput::make('slug')->required()->unique(ignoreRecord: true),
        Textarea::make('description')->label('Mô tả ngắn')->columnSpanFull(),
        RichEditor::make('content')->label('Nội dung')
            ->fileAttachmentsDisk('public')->fileAttachmentsDirectory('projects/content')->columnSpanFull(),
        FileUpload::make('images')->multiple()->reorderable()->image()
            ->disk('public')->directory('projects')->visibility('public')->maxFiles(20)
            ->columnSpanFull()->label('Hình ảnh dự án'),
        Toggle::make('is_active')->label('Kích hoạt')->default(true),
    ])
    ```

    **ProjectsTable.php** (Namespace: `App\Filament\Resources\Projects\Tables`):
    - Columns: TextColumn 'title' (searchable, sortable, limit 50), TextColumn 'description' (limit 80), ToggleColumn 'is_active' (label 'Kích hoạt'), TextColumn 'created_at' (dateTime, sortable)
    - Filters: TernaryFilter for 'is_active'
    - DefaultSort: 'created_at' desc
    - Actions: EditAction, DeleteAction
    - BulkActionGroup: DeleteBulkAction

    **ListProjects.php**: extends ListRecords, $resource = ProjectResource, header action = CreateAction
    **CreateProject.php**: extends CreateRecord, $resource = ProjectResource
    **EditProject.php**: extends EditRecord, $resource = ProjectResource
  </action>
  <verify>
    <automated>php artisan tinker --execute="new App\Filament\Resources\Projects\ProjectResource" 2>&1</automated>
  </verify>
  <done>ProjectResource with 6 modular files created. ProjectForm includes FileUpload multiple() for images gallery. Navigation sort = 5. Resource instantiates without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Create PostResourceTest and ProjectResourceTest</name>
  <files>
    tests/Feature/Filament/PostResourceTest.php,
    tests/Feature/Filament/ProjectResourceTest.php
  </files>
  <read_first>
    @tests/Feature/Filament/CategoryResourceTest.php (pattern for Filament resource tests)
  </read_first>
  <action>
    Per the Wave 0 scaffolding plan and Nyquist rule, create 2 test files:

    **PostResourceTest.php** (covers REQ-01):
    - Namespace `Tests\Feature\Filament`
    - Extends `Tests\TestCase`
    - Test methods:
      - `test_can_list_posts()`: Login as admin, get PostResource index page → 200
      - `test_can_create_post()`: POST to create with valid data → redirects, Post exists in DB
      - `test_can_edit_post()`: Update a post → changes persist
      - `test_can_delete_post()`: Delete a post → removed from DB
    - Use `actingAs(User::factory()->create())` for auth

    **ProjectResourceTest.php** (covers REQ-02):
    - Same test structure for Project CRUD
    - Test: can_list, can_create, can_edit, can_delete
    - For create, test with basic fields (omit images — FileUpload tests are visualized manually)
    - Verify Project::count() changes after operations

    Follow the same pattern as existing Filament resource tests in `tests/Feature/Filament/`.
  </action>
  <verify>
    <automated>php artisan test --filter=PostResourceTest --filter=ProjectResourceTest 2>&1</automated>
  </verify>
  <done>2 test files created. PostResourceTest covers CRUD operations for PostResource. ProjectResourceTest covers CRUD operations for ProjectResource. Tests pass with green output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → Filament Resource | Authenticated admin CRUD operations — trusted user |
| RichEditor upload → Storage | Image files uploaded via TinyMCE to public disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | RichEditor content | accept | Content authored by trusted admin only; XSS risk when rendered with `{!! !!}` on frontend but content is admin-originated |
| T-03-04 | Tampering | FileUpload images | mitigate | Filament FileUpload validates file type (image/*); Intervention pipeline resizes (if configured) — file type whitelist at upload |
</threat_model>

<verification>
1. PostResource loads in admin panel: navigate to `/admin` → sidebar shows "Tin tức" at position 4
2. ProjectResource loads in sidebar at position 5
3. Create a Post with RichEditor content → saves, displays, edits, deletes
4. Create a Project with image upload → images saved as JSON array, displays, edits, deletes
5. `php artisan test --filter=PostResourceTest --filter=ProjectResourceTest` passes
</verification>

<success_criteria>
- PostResource and ProjectResource created with modular pattern
- PostForm has RichEditor with image attachments (per D-02)
- ProjectForm has FileUpload multiple with reorderable for JSON gallery (per D-04)
- Navigation sort: Post=4, Project=5 (per D-13)
- All 6 page files created per resource
- CRUD tests pass for both resources
- `php artisan test --filter=Phase3` passes all Filament resource tests
</success_criteria>

<output>
Create `.planning/phases/03/03-02-SUMMARY.md` when done.
</output>
