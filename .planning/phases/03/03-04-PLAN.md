---
phase: 03
plan: 04
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - app/Http/Controllers/PostController.php
  - app/Http/Controllers/ProjectController.php
  - resources/views/posts/index.blade.php
  - resources/views/posts/show.blade.php
  - resources/views/projects/index.blade.php
  - resources/views/projects/show.blade.php
  - routes/web.php
autonomous: true
requirements:
  - REQ-05
  - REQ-06
  - REQ-12
user_setup: []

must_haves:
  truths:
    - "User can visit /tin-tuc and see paginated list of published posts"
    - "User can click a post title to view full post content with RichEditor HTML rendered"
    - "Post list shows post title, excerpt, image thumbnail, and published_at date"
    - "User can visit /du-an and see paginated list of active projects"
    - "User can click a project to view full project detail with image gallery"
    - "Project show page displays images in a gallery (reuse product gallery pattern)"
    - "Both lists use Bootstrap 5 styling consistent with template-frontend theme"
    - "URLs use Vietnamese slugs: /tin-tuc, /tin-tuc/{slug}, /du-an, /du-an/{slug}"
  artifacts:
    - path: "app/Http/Controllers/PostController.php"
      provides: "PostController with index() (paginated published posts) and show() (single post by slug)"
      exports: ["PostController"]
    - path: "app/Http/Controllers/ProjectController.php"
      provides: "ProjectController with index() (paginated active projects) and show() (single project by slug)"
      exports: ["ProjectController"]
    - path: "resources/views/posts/index.blade.php"
      provides: "Post listing page with pagination, cards showing title, excerpt, image, date"
      min_lines: 40
    - path: "resources/views/posts/show.blade.php"
      provides: "Post detail page with full content, breadcrumb navigation"
      min_lines: 40
    - path: "resources/views/projects/index.blade.php"
      provides: "Project listing page with pagination, cards showing title, description, image"
      min_lines: 40
    - path: "resources/views/projects/show.blade.php"
      provides: "Project detail page with image gallery, content, breadcrumb"
      min_lines: 50
    - path: "routes/web.php"
      provides: "Route definitions for Post and Project frontend pages"
      contains: "tin-tuc\|du-an"
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Controllers/PostController.php"
      via: "Route::get('tin-tuc', ...) and Route::get('tin-tuc/{slug}', ...)"
      pattern: "PostController"
    - from: "routes/web.php"
      to: "app/Http/Controllers/ProjectController.php"
      via: "Route::get('du-an', ...) and Route::get('du-an/{slug}', ...)"
      pattern: "ProjectController"
    - from: "app/Http/Controllers/PostController.php"
      to: "app/Models/Post.php"
      via: "Post::where('is_published', true)->paginate() and Post::where('slug', $slug)->firstOrFail()"
      pattern: "Post::"
    - from: "app/Http/Controllers/ProjectController.php"
      to: "app/Models/Project.php"
      via: "Project::where('is_active', true)->paginate() and Project::where('slug', $slug)->firstOrFail()"
      pattern: "Project::"
    - from: "resources/views/posts/show.blade.php"
      to: "app/Models/Post.php"
      via: "Renders $post->title, $post->content (with {!! !!}), $post->image"
      pattern: "\\$post->"
---

<objective>
**Phase 3, Plan 04:** Frontend Pages for Posts and Projects.

**Purpose:** Create public-facing Blade views for blog/news listing, blog detail, project listing, and project detail with image gallery. Build controllers with pagination. Add routes.

**Output:** 2 Controllers, 4 Blade views, route updates.
</objective>

<execution_context>
@C:/Users/daotu/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/daotu/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03/03-CONTEXT.md
@.planning/phases/03/03-RESEARCH.md

@app/Http/Controllers/ProductController.php
@resources/views/products/show.blade.php (for Blade patterns and gallery JS)
@resources/views/products/index.blade.php (for index page pattern)
@resources/views/layouts/app.blade.php
@template-frontend/blog.html (design reference for news listing)
@routes/web.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PostController with paginated index and slug-based show, plus Blade views</name>
  <files>
    app/Http/Controllers/PostController.php,
    resources/views/posts/index.blade.php,
    resources/views/posts/show.blade.php
  </files>
  <read_first>
    @app/Http/Controllers/ProductController.php (pattern for controller structure)
    @resources/views/products/show.blade.php (page header pattern, breadcrumb, sections)
    @template-frontend/blog.html (design layout reference)
  </read_first>
  <action>
    Per D-22 (new Blade views), D-24 (pagination).

    **PostController.php** (Namespace: `App\Http\Controllers`):
    ```php
    class PostController extends Controller
    {
        public function index()
        {
            $posts = Post::where('is_published', true)
                ->orderBy('published_at', 'desc')
                ->paginate(9);
            return view('posts.index', compact('posts'));
        }

        public function show(string $slug)
        {
            $post = Post::where('slug', $slug)
                ->where('is_published', true)
                ->firstOrFail();
            return view('posts.show', compact('post'));
        }
    }
    ```
    Import `App\Models\Post`.

    **posts/index.blade.php**:
    - Extends `layouts.app`, section 'title' = "Tin tức"
    - Page Header section (matching existing site pattern): breadcrumb Trang chủ → Tin tức
    - Post grid: row with col-lg-4 col-md-6 (3 columns on desktop, 2 on tablet)
    - Each post card shows:
      - Image thumbnail (with `asset('storage/' . $post->image)` fallback to placeholder)
      - Title as link to `route('posts.show', $post->slug)`
      - Excerpt (truncated to ~120 chars)
      - Published date formatted
    - Pagination links: `$posts->links()` with Bootstrap 5 pagination styling
    - Empty state: "Chưa có bài viết nào" if no posts
    - Use existing `wow fadeInUp` animations consistent with the template
    - Consistent with `template-frontend/blog.html` layout

    **posts/show.blade.php**:
    - Extends `layouts.app`, title = $post->title
    - Page Header with breadcrumb: Trang chủ → Tin tức → Post title
    - Main content area (col-lg-8 or full-width):
      - Post title as h1
      - Published_at date
      - Featured image if exists
      - Content: `{!! $post->content !!}` — raw HTML from RichEditor (safe — admin authored per D-02)
      - Social sharing section or simple "Quay lại tin tức" link
    - Back navigation: `<a href="{{ route('posts.index') }}">« Quay lại danh sách tin tức</a>`
    - Meta tags: yield title and meta_description from post excerpt
  </action>
  <verify>
    <automated>php artisan tinker --execute="app(App\Http\Controllers\PostController::class)->index();" 2>&1</automated>
  </verify>
  <done>PostController with index() and show() methods created. Blade views posts/index and posts/show render with Bootstrap 5 theme. Pagination working.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProjectController with paginated index and slug-based show, plus Blade views</name>
  <files>
    app/Http/Controllers/ProjectController.php,
    resources/views/projects/index.blade.php,
    resources/views/projects/show.blade.php
  </files>
  <read_first>
    @resources/views/products/show.blade.php (for image gallery JS pattern — reuse for project gallery)
    @app/Models/Project.php (for images array cast)
  </read_first>
  <action>
    Per D-22 (new Blade views), D-24 (pagination), D-04 (images JSON gallery).

    **ProjectController.php** (Namespace: `App\Http\Controllers`):
    ```php
    class ProjectController extends Controller
    {
        public function index()
        {
            $projects = Project::where('is_active', true)
                ->orderBy('created_at', 'desc')
                ->paginate(9);
            return view('projects.index', compact('projects'));
        }

        public function show(string $slug)
        {
            $project = Project::where('slug', $slug)
                ->where('is_active', true)
                ->firstOrFail();
            return view('projects.show', compact('project'));
        }
    }
    ```

    **projects/index.blade.php**:
    - Extends `layouts.app`, title = "Dự án"
    - Page Header with breadcrumb: Trang chủ → Dự án
    - Project grid: row with col-lg-4 col-md-6 cards
    - Each card shows:
      - First image from `$project->images[0]` array as thumbnail (with fallback placeholder)
      - Project title as link to `route('projects.show', $project->slug)`
      - Description (truncated)
      - "Xem chi tiết" button/link
    - Pagination links with Bootstrap 5
    - Empty state: "Chưa có dự án nào"

    **projects/show.blade.php**:
    - Extends `layouts.app`, title = $project->title
    - Page Header with breadcrumb: Trang chủ → Dự án → Project title
    - Two-column layout:
      - Left (col-lg-7): Image gallery — reuse the existing gallery HTML/JS from `products/show.blade.php`
        - Main image display with prev/next navigation
        - Thumbnail strip with click-to-select
        - Reuse the `galleryImages` JS array, `selectImage()`, `prevImage()`, `nextImage()` pattern
        - Images served via `asset('storage/' . $image)`
      - Right (col-lg-5): Project info sidebar
        - Title, description
        - Full content rendered with `{!! $project->content !!}` (admin-authored RichEditor content)
    - Back navigation: `<a href="{{ route('projects.index') }}">« Quay lại danh sách dự án</a>`
  </action>
  <verify>
    <automated>php artisan tinker --execute="app(App\Http\Controllers\ProjectController::class)->index();" 2>&1</automated>
  </verify>
  <done>ProjectController with index() and show(). Blade views projects/index and projects/show with working gallery (reused product gallery JS). Pagination working.</done>
</task>

<task type="auto">
  <name>Task 3: Add routes for posts and projects, create frontend tests</name>
  <files>
    routes/web.php,
    tests/Feature/Http/PostFrontendTest.php,
    tests/Feature/Http/ProjectFrontendTest.php
  </files>
  <read_first>
    @routes/web.php (current state — need to add PostController and ProjectController routes)
  </read_first>
  <action>
    **routes/web.php update:**
    Add imports and routes after existing route definitions:
    ```php
    use App\Http\Controllers\PostController;
    use App\Http\Controllers\ProjectController;

    Route::get('tin-tuc', [PostController::class, 'index'])->name('posts.index');
    Route::get('tin-tuc/{slug}', [PostController::class, 'show'])->name('posts.show');

    Route::get('du-an', [ProjectController::class, 'index'])->name('projects.index');
    Route::get('du-an/{slug}', [ProjectController::class, 'show'])->name('projects.show');
    ```
    Place these after product routes, before the contact route.
    Route names follow existing convention: `posts.index`, `posts.show`, `projects.index`, `projects.show`.

    **PostFrontendTest.php** (covers REQ-05):
    - Namespace `Tests\Feature\Http`
    - `test_posts_index_page_renders()`: GET `/tin-tuc` → 200, see "Tin tức" text
    - `test_posts_index_shows_published_posts()`: Create a published post → appears on index
    - `test_posts_index_hides_unpublished_posts()`: Create unpublished post → does not appear
    - `test_post_show_page_renders()`: GET `/tin-tuc/{slug}` for published post → 200, see title
    - `test_post_show_returns_404_for_unpublished()`: GET unpublished post slug → 404

    **ProjectFrontendTest.php** (covers REQ-06):
    - `test_projects_index_page_renders()`: GET `/du-an` → 200
    - `test_projects_index_shows_active_projects()`: Active project appears
    - `test_projects_index_hides_inactive_projects()`: Inactive project hidden
    - `test_project_show_page_renders()`: GET `/du-an/{slug}` for active project → 200
    - `test_project_show_returns_404_for_inactive()`: Inactive project → 404

    Use `Post::create()` and `Project::create()` in tests to seed data.
  </action>
  <verify>
    <automated>php artisan test --filter=PostFrontendTest --filter=ProjectFrontendTest 2>&1</automated>
  </verify>
  <done>Routes added. PostFrontendTest and ProjectFrontendTest created and passing. Posts/projects accessible at /tin-tuc and /du-an.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public → Frontend Pages | Unauthenticated users access public content — no sensitive data exposed |
| Database → Blade render | Admin-authored content (RichEditor HTML) rendered with {!! !!} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing/XSS | Post/Project content (`{!! !!}`) | accept | Content is admin-authored via trusted Filament panel with RichEditor; only trusted users create/edit content |
| T-03-08 | Information Disclosure | Unpublished posts | mitigate | Controller query adds `where('is_published', true)` / `where('is_active', true)` — unpublished content never exposed publicly |
</threat_model>

<verification>
1. Visit `/tin-tuc` — post list renders with pagination, only published posts shown
2. Visit `/tin-tuc/{slug}` — post detail renders with content, breadcrumb, back link
3. Visit `/du-an` — project list renders with pagination, only active projects shown
4. Visit `/du-an/{slug}` — project detail renders with image gallery (prev/next, thumbnails)
5. Unpublished/inactive items return 404 on show pages
6. `php artisan test --filter=PostFrontendTest --filter=ProjectFrontendTest` passes
</verification>

<success_criteria>
- PostController + ProjectController created with index (paginated) and show (by slug) methods
- 4 Blade views created: posts/index, posts/show, projects/index, projects/show
- Routes added for /tin-tuc and /du-an with Vietnamese slugs
- Project show page includes working image gallery (reused product gallery JS pattern)
- Pagination on both list pages (9 items per page — agent discretion)
- Frontend tests pass for both controllers
</success_criteria>

<output>
Create `.planning/phases/03/03-04-SUMMARY.md` when done.
</output>
